# What the dropped domains cost, file by file

> GENERATED by `node tools-frontend-retired-inventory.mjs --write`. Do not edit by hand.

208 entity call sites across 86 files have no destination
in the owned store: 203 reach a domain the migration decided
not to carry, and 5 are writes to D83 reference tables it
carries for reads only. This says what each file loses, so the hiding work is a
series of per-screen decisions rather than one decision about 208
numbers. It claims nothing about whether a screen is safe to hide: a file
marked **whole** has no surviving data of its own, and every other file keeps
some and needs reading.

Reads 123, writes 84, subscriptions 1. 59 of the 86 files lose everything they read.

## By entity — what was decided

| Entity | Sites | Files | What it becomes |
| --- | ---: | ---: | --- |
| `TrainingCourse` | 28 | 15 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `TrainingAssignment` | 21 | 21 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `OASISUpload` | 17 | 13 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `TrainingModule` | 15 | 12 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `LearningPlanCourse` | 12 | 3 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `FaxContact` | 11 | 3 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `LearningPlan` | 10 | 5 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `TrainingCertificate` | 10 | 10 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `FaxLog` | 7 | 5 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `OASISAudit` | 7 | 5 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `OASISAutomationRule` | 7 | 3 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `MicroLearningProgress` | 6 | 5 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `TrainingQuestion` | 6 | 4 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `FaxTemplate` | 5 | 1 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `CallLog` | 4 | 2 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `FacilityDocumentationRule` | 4 | 1 | the broker family serves this entity read-only, so the write has no destination |
| `TrainingAttempt` | 4 | 4 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `Announcement` | 3 | 1 | the broker family serves this entity read-only, so the write has no destination |
| `OASISActionItem` | 3 | 2 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `OASISAssessment` | 3 | 3 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `OASISScenario` | 3 | 1 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `OASISWorkflowExecution` | 3 | 2 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `PlanEnrollment` | 3 | 3 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `ComplianceRule` | 2 | 1 | D83 reference data: the table is written by migration and grants no caller role anything, so the write is refused by the GRANT rather than by a policy |
| `FaxRetryConfig` | 2 | 1 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `MedicareComplianceRule` | 2 | 1 | D83 reference data: the table is written by migration and grants no caller role anything, so the write is refused by the GRANT rather than by a policy |
| `OASISFeedback` | 2 | 2 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `RegulatoryUpdate` | 2 | 1 | the broker family serves this entity read-only, so the write has no destination |
| `TrainingRecommendation` | 2 | 2 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `TrainingTemplate` | 2 | 1 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |
| `MedicareGuideline` | 1 | 1 | D83 reference data: the table is written by migration and grants no caller role anything, so the write is refused by the GRANT rather than by a policy |
| `PhoneNumber` | 1 | 1 | no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7) |

## By file — what each screen loses

### `src/components/training/CourseManager.jsx` — **whole**

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (create, delete, filter, list, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingModule` (create, filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingQuestion` (create, filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/AnnualLearningPlanPanel.jsx` — **whole**

- `LearningPlan` (create, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `LearningPlanCourse` (create, delete, filter, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/LearningPlanManager.jsx`

- `LearningPlan` (delete, list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `LearningPlanCourse` (create, delete, filter, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/AIComplianceInServicesHub.jsx`

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCertificate` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (create, list, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingTemplate` (create, list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/AnnualMandatoryEducationHub.jsx`

- `LearningPlan` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCertificate` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (create, list, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/fax/FaxAddressBook.jsx` — **whole**

- `FaxContact` (bulkCreate, create, delete, list, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/fax/FaxTemplateManager.jsx` — **whole**

- `FaxTemplate` (create, delete, list, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/hub-tabs/FaxContacts.jsx` — **whole**

- `FaxContact` (bulkCreate, create, delete, list, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/OASISAutomationSettings.jsx` — **whole**

- `OASISAutomationRule` (create, delete, list, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/MyTrainingDashboard.jsx` — **whole**

- `PlanEnrollment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAttempt` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCertificate` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/SMEReviewQueue.jsx` — **whole**

- `TrainingCourse` (filter, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingModule` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingQuestion` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/admin/FacilityDocumentationRulesManager.jsx`

- `FacilityDocumentationRule` (create, delete, update) — the broker family serves this entity read-only, so the write has no destination

### `src/components/compliance/RegulatoryMonitor.jsx`

- `ComplianceRule` (create, update) — D83 reference data: the table is written by migration and grants no caller role anything, so the write is refused by the GRANT rather than by a policy
- `RegulatoryUpdate` (create, update) — the broker family serves this entity read-only, so the write has no destination

### `src/components/training/LearningPathProgress.jsx` — **whole**

- `LearningPlan` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `LearningPlanCourse` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCertificate` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/MyAnnualEducationDashboard.jsx` — **whole**

- `PlanEnrollment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCertificate` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/hooks/useMyTrainingCompletions.js` — **whole**

- `MicroLearningProgress` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAttempt` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCertificate` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/pages/LearningCenter.jsx` — **whole**

- `PlanEnrollment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCertificate` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/pages/TrainingCoursePlayer.jsx` — **whole**

- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingModule` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingQuestion` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/admin/AnnouncementManager.jsx`

- `Announcement` (create, delete, update) — the broker family serves this entity read-only, so the write has no destination

### `src/components/hub-tabs/OASISAnalyzer.jsx` — **whole**

- `OASISUpload` (create, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/OASISScenarioManager.jsx` — **whole**

- `OASISScenario` (create, delete, filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/WorkflowExecutionEngine.jsx`

- `OASISAutomationRule` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `OASISWorkflowExecution` (create, filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/LearnerMemoryBoosters.jsx` — **whole**

- `MicroLearningProgress` (create, filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/TrainingAttachmentManager.jsx` — **whole**

- `TrainingCourse` (update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingModule` (filter, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/TrainingVideoStudio.jsx` — **whole**

- `TrainingCourse` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingModule` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/pages/AdminTraining.jsx`

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAttempt` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/pages/ManagerSkillGapDashboard.jsx`

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingAttempt` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/admin/FaxRetryConfigPanel.jsx` — **whole**

- `FaxRetryConfig` (create, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/compliance/MedicareRuleSeeder.jsx`

- `MedicareComplianceRule` (create, update) — D83 reference data: the table is written by migration and grants no caller role anything, so the write is refused by the GRANT rather than by a policy

### `src/components/fax/EnhancedFaxHistory.jsx` — **whole**

- `FaxLog` (filter, list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/fax/RealtimeFaxStatusTracker.jsx` — **whole**

- `FaxLog` (list, subscribe) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/hub-tabs/NurseTraining.jsx` — **whole**

- `MicroLearningProgress` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingRecommendation` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/hub-tabs/OASISAuditDashboard.jsx`

- `OASISAudit` (list, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/learning/CourseRosterReport.jsx` — **whole**

- `TrainingAssignment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingCourse` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/AIAuditRiskPredictor.jsx` — **whole**

- `OASISAudit` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `OASISUpload` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/ComprehensiveOASISReviewer.jsx` — **whole**

- `OASISActionItem` (bulkCreate, filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/OASISApprovalWorkflow.jsx` — **whole**

- `OASISUpload` (filter, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/OASISAutoFlagger.jsx` — **whole**

- `OASISAudit` (create, filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/PDGMAnalyticsDashboard.jsx` — **whole**

- `OASISActionItem` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `OASISUpload` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/PredictiveOutcomesAnalyzer.jsx`

- `OASISUpload` (filter, list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/WorkflowMonitoringDashboard.jsx` — **whole**

- `OASISAutomationRule` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `OASISWorkflowExecution` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/CourseForm.jsx`

- `TrainingCourse` (create, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/LearningPlanForm.jsx` — **whole**

- `LearningPlan` (create, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/voice/CallHistoryList.jsx` — **whole**

- `CallLog` (filter, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/voice/CallbackQueue.jsx` — **whole**

- `CallLog` (filter, update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/pages/AdminTrainingAnalytics.jsx`

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)
- `TrainingModule` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/admin/AdminOnboardingChecklistStrip.jsx`

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/admin/NumberPoolPanel.jsx`

- `PhoneNumber` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/clinical/OASISQuickUpdate.jsx` — **whole**

- `OASISAssessment` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/compliance/AIComplianceAuditor.jsx`

- `OASISUpload` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/fax/FaxRecipientFields.jsx`

- `FaxContact` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/fax/FaxSearchInterface.jsx` — **whole**

- `FaxLog` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/hub-tabs/ComplianceMonitoringDashboard.jsx`

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/hub-tabs/FaxAnalytics.jsx` — **whole**

- `FaxLog` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/hub-tabs/FaxLogsDashboard.jsx` — **whole**

- `FaxLog` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/hub-tabs/OASISAnalyticsDashboard.jsx` — **whole**

- `OASISUpload` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/learning/AnnualTranscriptCenter.jsx` — **whole**

- `TrainingCertificate` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/learning/CertificateExpirationReport.jsx`

- `TrainingCertificate` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/learning/CourseCatalogDetail.jsx` — **whole**

- `TrainingModule` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/learning/EmployeeTranscriptCenter.jsx` — **whole**

- `TrainingCertificate` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/learning/EnrollmentSummaryDashboard.jsx` — **whole**

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/learning/OverdueRemindersReport.jsx` — **whole**

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/AIGroupAssignmentValidator.jsx` — **whole**

- `OASISAudit` (create) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/AIProactiveOASISAssistant.jsx`

- `OASISUpload` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/AdvancedComplianceAnalyzer.jsx`

- `OASISUpload` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/AutomatedPDGMNavigator.jsx` — **whole**

- `OASISUpload` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/OASISAuditReportGenerator.jsx` — **whole**

- `OASISAudit` (update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/OASISComparisonView.jsx` — **whole**

- `OASISUpload` (update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/OASISPDFComparison.jsx` — **whole**

- `OASISFeedback` (create) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/PDGMTrendDashboard.jsx` — **whole**

- `OASISUpload` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/oasis/PatientMatchSelector.jsx` — **whole**

- `OASISFeedback` (create) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/reports/OASISComplianceReport.jsx`

- `OASISAssessment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/reports/PDGMReimbursementReport.jsx` — **whole**

- `OASISAssessment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/AIComplianceQuizGenerator.jsx` — **whole**

- `MicroLearningProgress` (create) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/CourseLessonBuilder.jsx` — **whole**

- `TrainingModule` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/CourseQuizBuilder.jsx` — **whole**

- `TrainingQuestion` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/DeficitAnalyzer.jsx` — **whole**

- `TrainingRecommendation` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/InteractiveDocumentationScenarios.jsx` — **whole**

- `MicroLearningProgress` (create) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/ModuleScriptPanel.jsx` — **whole**

- `TrainingModule` (update) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/OnboardingTracker.jsx` — **whole**

- `TrainingModule` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/StaffEducationComplianceReport.jsx`

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/components/training/TrainingLibrary.jsx` — **whole**

- `TrainingModule` (filter) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/pages/ComplianceCenter.jsx`

- `TrainingAssignment` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/pages/MedicareGuidelinesLibrary.jsx`

- `MedicareGuideline` (update) — D83 reference data: the table is written by migration and grants no caller role anything, so the write is refused by the GRANT rather than by a policy

### `src/pages/NurseTrainingHub.jsx` — **whole**

- `TrainingModule` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

### `src/pages/PredictiveAnalytics.jsx`

- `OASISUpload` (list) — no table in the owned store — the domain moves to the Hub (D8) or stays paused (D7)

