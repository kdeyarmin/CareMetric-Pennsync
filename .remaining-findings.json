{
 "src/components/oasis/EnhancedPDGMCaseMixAnalyzer.jsx": [
  {
   "file": "src/components/oasis/EnhancedPDGMCaseMixAnalyzer.jsx",
   "symbol": "performCaseMixAnalysis (catch block)",
   "summary": "An AI failure leaves caseMixAnalysis null with no error state, so the card renders \"Loading case-mix analysis...\" indefinitely and the effect never re-fires; only a page reload recovers.",
   "real": true,
   "reason": "Confirmed at EnhancedPDGMCaseMixAnalyzer.jsx:166-169 (catch only console.errors and toasts; setCaseMixAnalysis is never called) and the render fallback at lines 193-214, where the `!caseMixAnalysis` branch shows the literal string \"Loading case-mix analysis...\" whenever ai.loading is false. The effect at lines 174-178 depends on [pdgmData, navigationData, caseMixAnalysis, performCaseMixAnalysis]; performCaseMixAnalysis is a useCallback over [pdgmData, navigationData] (line 172), so after a failure none of the deps change and nothing retries. useAICall (src/hooks/useAICall.js) does expose an `error` value and already retries internally via runWithRetry, but this component destructures only run/loading and never renders ai.error, so there is no error surface and no retry affordance. Mounted from OASISRevenueAnalysis.jsx:56 and OASISAnalyzer.jsx:2220. Mitigating factor: a toast does fire at the moment of failure, so the user is not completely uninformed - the defect is the permanently misleading \"Loading\" text and the absent retry.",
   "corrected_severity": "low",
   "corrected_fix": "No new state is needed - useAICall already tracks the error. In EnhancedPDGMCaseMixAnalyzer.jsx, inside the `if (!caseMixAnalysis)` block (lines 202-211), add an error branch before the loading check: render `ai.error` as a failure message plus a Retry button whose onClick calls performCaseMixAnalysis(). Concretely, replace the ternary at lines 203-210 with a three-way: `ai.loading ? (<spinner/>) : ai.error ? (<><p className=\"text-sm text-red-600 text-center mb-2\">Case-mix analysis failed to load.</p><Button variant=\"outline\" size=\"sm\" className=\"mx-auto block\" onClick={performCaseMixAnalysis}>Retry</Button></>) : (<p ...>Loading case-mix analysis...</p>)`. Note ai.run already clears error on the next call, so the retry path self-heals; import Button if not already imported."
  },
  {
   "file": "src/components/oasis/EnhancedPDGMCaseMixAnalyzer.jsx",
   "symbol": "EnhancedPDGMCaseMixAnalyzer (optimization opportunities render)",
   "summary": "Array.prototype.sort is called directly on caseMixAnalysis.optimization_opportunities during render, mutating the React state object in place.",
   "real": true,
   "reason": "Confirmed verbatim at EnhancedPDGMCaseMixAnalyzer.jsx:344-346. caseMixAnalysis is state (line 17) set from the AI result (line 165), so .optimization_opportunities is the same array reference on every render and .sort() reorders it in place during render - a genuine impure render. I could not refute the mutation itself; there is no defensive copy anywhere between the AI response and this line. However the claimed user-visible harm is largely theoretical: the comparator is deterministic, so repeated sorts are idempotent, the component is the only consumer of this state, and caseMixAnalysis is not passed to any export/PDF path (the component takes only pdgmData and navigationData as props and renders locally). Real defect, negligible impact.",
   "corrected_severity": "low",
   "corrected_fix": "In src/components/oasis/EnhancedPDGMCaseMixAnalyzer.jsx line 344, change `{caseMixAnalysis.optimization_opportunities` to `{[...caseMixAnalysis.optimization_opportunities]` so the sort at line 345 operates on a copy. Nothing else changes; the rendered order is identical."
  }
 ],
 "src/components/smartNote/StructuredNoteDrafter.jsx": [
  {
   "file": "src/components/smartNote/StructuredNoteDrafter.jsx",
   "symbol": "useInNoteBuilder",
   "summary": "The weight regex at line 111 omits the kg spelling variants, the leading \\b, and the trailing (?!\\d) that its own comment claims to mirror from extractVitals, so a kg-documented weight is written into the handoff note as pounds and non-weight/typo'd numbers are captured as plausible weights.",
   "real": true,
   "reason": "Could not refute. (1) The code exists verbatim at lines 111 and 115 under a comment (107-109) explicitly claiming to mirror extractVitals. (2) It does not: factExtraction.js:127 and visitComparison.js:71 both use /\\b(?:wt|weight)\\s*:?\\s*(\\d{2,3}(?:\\.\\d)?)(?!\\d)\\s*(lbs?|pounds?|kgs?|kilograms?)?\\b/i, and visitComparison tests the unit with /^k/i, not /^kg$/i. factExtraction.js:124-126 documents the \\b and (?!\\d) as deliberately guarding the exact 'underweight 15%' and 'weight 1800' cases. (3) Executed the quoted regex: 'Weight 80 kilograms per bath scale.' -> group2 undefined, so no conversion runs and 80 is appended as lbs (real value 176.4, a 96 lb error); 'Weight 1800' -> 180; 'underweight 15%' -> 15. All reach `if (weight)` at line 117. (4) Reachable: the draft textarea at 248-252 is user-editable and all four free-text fields flow verbatim into draft via generate() (70-74), so arbitrary prose hits the regex; SmartNoteAssistant.jsx:504-509 pipes the handoff into setNote for the compliance pipeline. (5) No downstream guard: toCanonicalVitalSigns intentionally drops weight from canonical vital_signs (factExtraction.js:172-173), so this text line is the sole weight carrier. Worse, because line 117 emits an explicit unit ('Weight: 80 lbs.'), the kg-suppression at factExtraction.js:339 does not fire and MEASUREMENT_PATTERNS (line 272) whitelists '80lbs' for the value-guard \u2014 precisely the failure the comment at 331-338 says that suppression exists to prevent. (6) No test covers it: there is no StructuredNoteDrafter test file, and factExtraction.test.js only exercises toCanonicalVitalSigns and the grid/text merge (lines 197, 251), never useInNoteBuilder's weight regex.",
   "corrected_severity": "medium",
   "corrected_fix": "In /home/user/CareMetric-Pennsync/src/components/smartNote/StructuredNoteDrafter.jsx, align the regex and unit test with the canonical versions in factExtraction.js:127 / visitComparison.js:71-73 (two lines, no other behavior change):\n\nLine 111, change:\n    const wtMatch = draft.match(/(?:wt|weight)\\s*:?\\s*(\\d{2,3}(?:\\.\\d)?)\\s*(lbs?|kg)?/i);\nto:\n    const wtMatch = draft.match(/\\b(?:wt|weight)\\s*:?\\s*(\\d{2,3}(?:\\.\\d)?)(?!\\d)\\s*(lbs?|pounds?|kgs?|kilograms?)?\\b/i);\n\nLine 115, change:\n      if (/^kg$/i.test(wtMatch[2] || \"\")) weight = Math.round(weight * 2.20462 * 10) / 10;\nto:\n      if (/^k/i.test(wtMatch[2] || \"\")) weight = Math.round(weight * 2.20462 * 10) / 10;\n\nVerified against the failing inputs: 'Weight 80 kilograms'/'80 kgs'/'80 kg' now capture the unit and convert to 176.4; 'Weight 1800' and 'underweight 15%' no longer match at all, so weight correctly falls back to gridWeight (null) and no fabricated Weight line is appended. Existing correct inputs ('Weight 180 lbs', 'wt 176.4 lbs') are unaffected."
  }
 ],
 "src/components/smartNote/compliance/ruleLibrary.js": [
  {
   "file": "src/components/smartNote/compliance/ruleLibrary.js",
   "symbol": "enrich",
   "summary": "enrich() unions a rule's keywords into whatever element its category maps to, so two default rules sharing a CATEGORY_TO_ID target let one rule's generic keyword satisfy the other rule's requirement.",
   "real": true,
   "reason": "Reproduced. buildMergedElements(DEFAULT_MEDICARE_RULES, {serviceLine:'home_health', visitType:'recertification'}) yields care_plan_progress.keywords = ['goal','progress','plan of care','improving','toward goal','plan of treatment','review','recertification','physician review'] \u2014 'review' and 'recertification' come from 'PA Plan of Treatment Review' (category plan_of_care) merging into the element 'Plan of Care Adherence and Progress' already owns. detectPresence then scores 'Medication review completed with the patient.' and 'Reviewed the med list.' as present:true for 'Progress toward care-plan goals'. Same collision on coordination_of_care -> idg_coordination: keywords become [...,'aide supervision','supervisory visit','home health aide'] under the label 'Coordination of Care', and 'Home health aide is on the case.' scores present:true, while the PA aide rule's own question/remediation are dropped (enrich only sets remediationGuidance when unset) yet the rule is still pushed to `applied` and stamped into ComplianceAudit.rule_versions. Reachable in production: MedicareRuleSeeder.jsx seeds DEFAULT_MEDICARE_RULES, and ConstrainedNoteReviewer.jsx:203 calls buildOverrides(complianceRules, ...). ruleLibrary.test.js and defaultMedicareRules.test.js only test single-rule enrichment, severity raising, service-line scoping and infection_control addition \u2014 no test covers two rules on one category. Downgraded from 'high': the colliding elements are severity 'required', not critical, so nothing hard-blocks; the damage is an inflated coverage score, a skipped nurse question, and a mis-labelled rule_versions stamp.",
   "corrected_severity": "medium",
   "corrected_fix": "Minimal surgical change in src/components/smartNote/compliance/ruleLibrary.js: stop a SECOND rule from injecting keywords into an element another rule already enriched.\n1. Change the signature at line 72 to `function enrich(element, rule, sev, mayMergeKeywords)` and gate line 73: `if (mayMergeKeywords && Array.isArray(rule.keywords) && rule.keywords.length) {`.\n2. In buildMergedElements, declare `const keywordOwner = new Set();` next to `const byId` (line 115).\n3. At line 124-130 replace the branch with:\n     if (existing) { enrich(existing, rule, sev, !keywordOwner.has(id)); }\n     else { const created = createElement(id, rule, sev); byId.set(id, created); elements.push(created); }\n     keywordOwner.add(id);\nSeverity raising, examples and remediationGuidance still merge from every matching rule (additive-only contract preserved), and the single-rule enrichment test at ruleLibrary.test.js:23 still passes because the first rule per id keeps merging."
  }
 ],
 "src/components/smartNote/compliance/reportingFields.js": [
  {
   "file": "src/components/smartNote/compliance/reportingFields.js",
   "symbol": "buildAuditFields",
   "summary": "acknowledgment is conditionally omitted from the audit-field patch, so a re-save that resolves the conflict leaves a stale override trail on the ComplianceAudit record.",
   "real": true,
   "reason": "Confirmed by reading the code path end to end. reportingFields.js:159 is literally `...(acknowledgment ? { acknowledgment } : {})`, i.e. the key is absent when null \u2014 while the comment immediately below (lines 161-163) states that partial ComplianceAudit updates only write present fields and that omission would leave a stale stamp, which is exactly why rule_versions is always emitted. The clearing path is reachable: ConstrainedNoteReviewer.jsx:377 builds `acknowledgment = critical.length ? {...} : null`, so once the nurse removes the offending medication there are no critical chart findings and acknowledgment is null; persistVisitNote.js:84-93 then produces ackSources = [] and acknowledgment = null, and persistVisitNote.js:190-192 calls base44.entities.ComplianceAudit.update(savedAuditId, auditFields) with the key missing. The same omission applies on the offlineClientRequestId re-save path (persistVisitNote.js:~222). No test refutes it \u2014 reportingFields.test.js:139 asserts the current omission behavior (that assertion encodes the bug, it does not disprove it), and persistVisitNote.spec.js stubs buildAuditFields entirely.",
   "corrected_severity": "medium",
   "corrected_fix": "src/components/smartNote/compliance/reportingFields.js line 159: replace `...(acknowledgment ? { acknowledgment } : {}),` with `acknowledgment: acknowledgment || null,` so the key is always present, matching how rule_versions is handled two lines below. Then update the now-inverted assertion in src/components/smartNote/compliance/reportingFields.test.js line 139 from `assert.ok(!(\"acknowledgment\" in out));` to `assert.equal(out.acknowledgment, null);` (and adjust its test name at line 170 / the surrounding wording, which currently says \"only when provided\"). No change needed in persistVisitNote.js \u2014 it already passes acknowledgment straight through."
  }
 ],
 "src/pages/SignDocument.jsx": [
  {
   "file": "src/pages/SignDocument.jsx",
   "symbol": "SignDocument (loading guard)",
   "summary": "Permanent 'Loading document...' card when the record query is disabled (no ?signature_id) or the record/patient never resolves",
   "real": true,
   "reason": "Confirmed reachable. src/pages/SignDocument.jsx:145-156 renders the loading card whenever `signatureRecord` is falsy, but the record query is `enabled: !!signatureId` (line 53), so with no ?signature_id it never runs and `signatureRecord` stays undefined forever \u2014 an indefinite 'Loading document...' with no error or empty state. SignDocument IS a routed manifest entry (src/lib/nav.manifest.js:296-304, category: null) and buildPaletteEntries (src/lib/nav.manifest.js:989-997) filters only on routed/adminOnly/superAdminOnly \u2014 it does not exclude category:null \u2014 so the command palette (src/components/navigation/CommandPalette.jsx:115) offers 'Sign Document' and navigates to /SignDocument with no query string. The same dead end occurs for a deleted/stale signature_id (filter returns [] -> select -> undefined). No test covers this path (there is no SignDocument spec). One correction to the claim: the page renders inside Layout (src/App.jsx:242), so the sidebar remains available \u2014 the user is not trapped, just shown a permanently misleading loading state, which is why this is low rather than medium.",
   "corrected_severity": "low",
   "corrected_fix": "In src/pages/SignDocument.jsx, take the loading flags from the queries: `const { data: signatureRecord, isLoading: recordLoading, isFetched: recordFetched } = useQuery({...})` (line 49) and `const { data: patient, isLoading: patientLoading } = useQuery({...})` (line 58). Then replace the single guard at line 145 with two branches: (1) `if (!signatureId || (recordFetched && !signatureRecord))` render a 'No document selected / document not found' card with a Button navigating to `createPageUrl('DocumentHub?tab=signatures')`; (2) keep the existing loading card but gate it on `recordLoading || (effectivePatientId && patientLoading) || !signatureRecord || !Array.isArray(signatureRecord.signers)`. No other behavior changes."
  }
 ],
 "src/pages/PatientEducationHub.jsx": [
  {
   "file": "src/pages/PatientEducationHub.jsx",
   "symbol": "handleDownload (error diagnostics logging)",
   "summary": "Frontend diagnostics write uses base44.asServiceRole, whose getter throws in the browser client, so the SystemLog row is never written",
   "real": true,
   "reason": "Confirmed. src/pages/PatientEducationHub.jsx:342 calls `await base44.asServiceRole.entities.SystemLog.create({...})`, and node_modules/@base44/sdk/dist/client.js:289-293 defines `get asServiceRole() { if (!serviceToken) throw new Error('Service token is required to use asServiceRole...'); }`. src/api/base44Client.js creates the client with appId/serverUrl/appBaseUrl/token/functionsVersion and NO serviceToken, so the getter throws synchronously on every invocation; the inner catch at lines 354-356 swallows it into a console.error. The diagnostic record can therefore never be written in any environment \u2014 dead instrumentation. Impact is limited to lost debugging telemetry: the surrounding user-facing error toast (line 359) still fires and nothing else breaks, so medium overstates it.",
   "corrected_severity": "low",
   "corrected_fix": "In src/pages/PatientEducationHub.jsx, replace `base44.asServiceRole.entities.SystemLog.create({...})` at line 342 with `base44.entities.SystemLog.create({...})` (same payload) so the write goes through the authenticated caller, or route it through an existing backend logging function. Keep the surrounding try/catch. If SystemLog RLS forbids non-admin writes, delete lines 340-356 instead so the block does not read as working instrumentation."
  }
 ],
 "src/routes.jsx": [
  {
   "file": "src/routes.jsx",
   "symbol": "pageModules (import.meta.glob)",
   "summary": "Lazy page glob matches *.spec.jsx / *.test.jsx, shipping test files into the production build",
   "real": true,
   "reason": "Confirmed. src/routes.jsx:24 is `const pageModules = import.meta.glob('./pages/*.jsx');` with no exclusions, and src/pages contains DuplicatePatients.spec.jsx, JoinTelehealth.a11y.test.jsx, PatientDetails.context.spec.jsx, PrivacyPolicy.a11y.test.jsx and ReferralTriage.integration.spec.jsx. The built output confirms the emission: dist/assets contains DuplicatePatients.spec-CTx8yZmr.js, JoinTelehealth.a11y.test-2Fr9R9g9.js, PatientDetails.context.spec-CBdwoEkY.js, PrivacyPolicy.a11y.test-B-gr_1UK.js, ReferralTriage.integration.spec-B4DVkxmd.js and testUtils-DafiIRvn.js (only the hashes differ from the claim, since dist is gitignored and rebuilt). Nothing in vite.config.js filters them. These chunks are never fetched at runtime (factoryFor only resolves manifest page names), so the impact is published test code, mock fixtures and testing-library glue on the public asset host plus wasted build output \u2014 build hygiene, not a functional defect.",
   "corrected_severity": "low",
   "corrected_fix": "Change src/routes.jsx line 24 to `const pageModules = import.meta.glob(['./pages/*.jsx', '!./pages/*.spec.jsx', '!./pages/*.test.jsx']);`. Nothing else changes \u2014 factoryFor still resolves manifest page names, and the excluded keys were never routed."
  }
 ],
 "src/components/patient/DuplicateScanner.jsx": [
  {
   "file": "src/components/patient/DuplicateScanner.jsx",
   "symbol": "evaluateAdvancedMatch / scanAndRemoveDuplicates",
   "summary": "Advanced scan auto-merges charts on 80% fuzzy first-name similarity + identical DOB \u2014 the twin/sibling pattern the canonical engine flags as POSSIBLE_TWINS \u2014 with no preview or confirmation.",
   "real": true,
   "reason": "Could not refute. Code at lines 94-100 is as quoted, and defaults enable the dangerous path with no user action: advancedOptions defaults `matchByNameAndDOB: true, fuzzyNameMatching: true` (lines 141-145). Arithmetic checks out against the real similarity() in patientDuplicateUtils.js:76-83 \u2014 levenshtein('jayden','kayden') = 1, maxLength 6 \u2192 83.33 >= 80; last names identical \u2192 100. score becomes 90 >= ADVANCED_MATCH_THRESHOLD (70), so the pair is grouped and Phase 2 calls `mergePatientInto(survivor.id, patient.id)` directly at line 260 with no dry-run gate \u2014 the `results.dry_run` \u2192 \"Confirm & merge\" button at lines 644-652 only ever exists for the standard (backend) mode, which alone returns dry_run. The canonical engine flags exactly this pair: patientDuplicateUtils.js:600-611 sets POSSIBLE_TWINS when levenshtein <= 1 but the first letters differ and similarity < 85 with matching last name + DOB; the advanced path never calls scorePatientPair, so that guard is bypassed. No test covers it \u2014 DuplicateScanner.spec.jsx (74 lines) has a single test, 'DuplicateScanner standard mode (dry-run + confirm)'. The merge is soft/recoverable (mergePatients.js archives with merged_into_id) but it reassigns every visit, OASIS, medication and incident of a live pediatric chart onto the sibling's chart first.",
   "corrected_severity": "high",
   "corrected_fix": "Minimal surgical fix in src/components/patient/DuplicateScanner.jsx: add `levenshtein` to the existing import from patientDuplicateUtils (line 22-26), then inside evaluateAdvancedMatch replace lines 94-96 with a twins-guarded fuzzy branch:\n\n  const twinsSuspect =\n    !!firstA && !!firstB && firstA !== firstB &&\n    !firstA.includes(firstB) && !firstB.includes(firstA) &&\n    similarity(firstA, firstB) < 85 &&\n    !(levenshtein(firstA, firstB) <= 1 && firstA[0] === firstB[0]);\n  const namesMatch = opts.fuzzyNameMatching\n    ? !twinsSuspect && similarity(firstA, firstB) >= 80 && similarity(lastA, lastB) >= 80\n    : !!firstA && firstA === firstB && sameLastName;\n\nThis mirrors the POSSIBLE_TWINS predicate in patientDuplicateUtils.js:600-610 exactly, leaves the exact-match, MRN, phone, email and address branches untouched, and keeps genuine typo variants ('Jon'/'John') matching. Follow-up (separate, larger change): give the advanced mode the same preview-then-confirm gate the standard mode has instead of merging inside the scan handler."
  },
  {
   "file": "src/components/patient/DuplicateScanner.jsx",
   "symbol": "scanAndRemoveDuplicates",
   "summary": "Failed mergePatientInto calls are logged to console but still reported as merged/removed, so the admin report overstates what actually happened.",
   "real": true,
   "reason": "Mostly confirmed; one detail in the claim's mechanism is wrong but the defect stands. The catch at lines 261-265 only console.errors, and `duplicateGroups.push({ ... removed: dupInfos.map(...) })` at lines 271-286 lists every duplicate unconditionally \u2014 the reported total is `patients_removed: duplicateGroups.reduce((sum, g) => sum + g.removed.length, 0)` (line 291), which is derived from `removed`, not from mergedCount, so the claim's statement that mergedCount feeds the report is inaccurate (mergedCount only drives the progress toast at lines 267-268). The user-visible outcome the claim describes is nonetheless real: the green 'Deduplication Complete! Found N duplicate group(s) and merged {results.patients_removed} duplicate record(s)' banner (line 547) and each 'Removed: <name>' row (line 616) include records whose merge threw \u2014 RLS denial on Patient.update, or the survivor-validation throws in mergePatients.js (`The surviving patient record was not found` / `is archived/merged`). Nothing surfaces the failure outside devtools, and the duplicates remain live on the roster. No test covers advanced mode.",
   "corrected_severity": "medium",
   "corrected_fix": "In src/components/patient/DuplicateScanner.jsx, track per-record outcome instead of assuming success. Around lines 258-269 replace the loop body with:\n\n  const mergedInfos = [];\n  let failedCount = 0;\n  for (const info of dupInfos) {\n    let ok = true;\n    try {\n      await mergePatientInto(survivor.id, info.patient.id, { mergedBy: null });\n    } catch (err) {\n      ok = false;\n      failedCount += 1;\n      console.error(`Merge failed for duplicate ${info.patient.id}:`, err?.message);\n    }\n    if (ok) mergedInfos.push(info);\n    mergedCount += 1; // progress counter only \u2014 keep counting attempts\n    ...existing progress toast...\n  }\n\nThen build the group from `mergedInfos` (line 277 `removed: mergedInfos.map(...)`, and the average_match_score at 283-285), skip pushing the group when mergedInfos is empty, accumulate failedCount across plans, add `merge_failures: totalFailed` to the setResults payload (lines 289-297), and render it in the completion Alert (line 547) plus a `toast.warning` when > 0."
  },
  {
   "file": "src/components/patient/DuplicateScanner.jsx",
   "symbol": "scanAndRemoveDuplicates (advanced mode)",
   "summary": "A duplicate whose mergePatientInto threw is still counted in mergedCount and still listed under `removed`, so the admin is told records were merged that are still live duplicates.",
   "real": true,
   "reason": "Verified at src/components/patient/DuplicateScanner.jsx:257-291. The catch at 260-264 only console.errors, and `mergedCount += 1` (line 265) plus the `removed: dupInfos.map(...)` push (lines 271-282) run unconditionally for every duplicate, success or failure. mergePatientInto genuinely throws on reachable paths \u2014 src/components/patient/mergePatients.js:83, 86, 94 and especially 97 (`The surviving patient record is archived/merged\u2026`), and the terminal `await base44.entities.Patient.update(duplicateId, { status: 'merged', is_archived: true, \u2026 })` at mergePatients.js:155-161 is NOT wrapped in try/catch, so an RLS-blocked or failed archive write propagates. That last case is the worst: related records have already been re-pointed to the survivor but the duplicate is never archived, so it stays active on the roster with its history stranded \u2014 exactly the state the summary claims was merged. The inflated number surfaces in the UI at line 549 (\"Found {duplicate_groups_found} duplicate group(s) and merged {patients_removed} duplicate record(s)\") via `patients_removed` computed from `removed.length` at line 291. No later reconciliation or re-verification exists.",
   "corrected_severity": "medium",
   "corrected_fix": "In the duplicate loop at src/components/patient/DuplicateScanner.jsx:257-269: introduce `const mergedInfos = []; const failedInfos = [];` before the loop over dupInfos; inside, set a local `let ok = true;` before the try and `ok = false;` inside the catch (keeping the console.error); after the try/catch do `if (ok) { mergedInfos.push(d); mergedCount += 1; } else { failedInfos.push(d); }`. Keep the progress toast driven by a separate `processed += 1` counter so progress still advances past failures. Then change line 277 to `removed: mergedInfos.map(d => ({ \u2026 }))` (unchanged mapping body) so `patients_removed` at line 291 counts only real merges, and push `failed: failedInfos.map(d => ({ name: `${d.patient.first_name} ${d.patient.last_name}`, mrn: d.patient.medical_record_number, id: d.patient.id }))` onto the group so the results panel can list what still needs retrying. `average_match_score` should be computed from mergedInfos with the existing empty-array guard."
  },
  {
   "file": "src/components/patient/DuplicateScanner.jsx",
   "symbol": "SURVIVOR_FIELDS / completenessScore",
   "summary": "`SURVIVOR_FIELDS` scores five field names that do not exist on the Patient entity and omits the backend's MRN/DOB weighting, so the client-side survivor choice does not mirror the backend `deduplicatePatients` rule its comment claims to mirror.",
   "real": true,
   "reason": "Code CONFIRMED at DuplicateScanner.jsx:33-57, and the comment at lines 46-47 does claim it 'mirrors the backend deduplicatePatients survivor rule so UI and server merges agree'. I grepped base44/entities/Patient.jsonc for city/state/zip/insurance_provider/gender \u2014 the only hit is an unrelated description string ('heat, water, electricity'); none exist as properties. `insurance_provider` appears nowhere else in src/ either, so those five names are dead and always contribute 0. The backend at deduplicatePatients/entry.ts:1011-1029 is genuinely different: medical_record_number +3, date_of_birth +2, plus a 27-field flat list including payor, allergies, current_medications, insurance_primary, insurance_secondary, clinical_notes that the frontend does not score. The claimed A-vs-B scenario checks out arithmetically (frontend A=2/B=6 picks B; backend A=8/B=6 picks A), so the two surfaces do select different survivors. `pickSurvivor` is live \u2014 used at line 215 in the advanced-scan merge path, which then soft-archives the losers via mergePatientInto. I could not refute it: engineSync.test.js pins only the embedded matching engine (findDuplicateGroups/normalizeName/REASON), not completenessScore, and DuplicateScanner.spec.jsx contains no survivor/completeness assertions. Severity is limited, though: autoMergeData defaults to true (line 146) and back-fills every non-system field from the losers into the survivor (lines 239-248), so in the default configuration no identifier is actually lost \u2014 only the surviving record's id/created_date differ between surfaces. With that checkbox unchecked the survivor can genuinely end up as the chart lacking MRN and DOB.",
   "corrected_severity": "low",
   "corrected_fix": "Surgical version in src/components/patient/DuplicateScanner.jsx: (1) replace SURVIVOR_FIELDS lines 33-37 with the real Patient properties, dropping the five nonexistent ones and adding the clinical/insurance fields the backend scores: [\"first_name\", \"last_name\", \"middle_name\", \"address\", \"phone\", \"email\", \"payor\", \"emergency_contact_name\", \"emergency_contact_phone\", \"physician_name\", \"physician_phone\", \"caregiver_name\", \"caregiver_email\", \"primary_diagnosis\", \"secondary_diagnoses\", \"allergies\", \"current_medications\", \"insurance_primary\", \"insurance_secondary\", \"admission_date\", \"care_type\", \"advance_directives\", \"functional_status\", \"assigned_nurses\", \"enhanced_notes_history\", \"clinical_notes\", \"goals_of_care\"] \u2014 MRN and DOB are removed from this list and weighted separately below. (2) Replace line 45 with the weighted form matching entry.ts:1011:\nconst completenessScore = (p) => {\n  if (!p) return 0;\n  let score = 0;\n  if (nonEmpty(p.medical_record_number)) score += 3;\n  if (nonEmpty(p.date_of_birth)) score += 2;\n  return SURVIVOR_FIELDS.reduce((n, k) => n + (nonEmpty(p[k]) ? 1 : 0), score);\n};\nLeave the archived/active tiebreakers at lines 52-55 and the back-fill logic unchanged. (Optional nit: `nonEmpty` treats a whitespace-only string as populated where the backend's `isPopulated` trims; trimming strings in `nonEmpty` would make the two identical.)"
  }
 ],
 "src/components/patient/PatientSummaryGenerator.jsx": [
  {
   "file": "src/components/patient/PatientSummaryGenerator.jsx",
   "symbol": "generateSummary (handoff format)",
   "summary": "An undocumented allergies field is sent to the model as the affirmative clinical negative 'NKDA', so generated handoff/summary documents assert no known drug allergies for patients whose allergy status is simply unknown.",
   "real": true,
   "reason": "Could not refute. Line 120 is `Allergies: ${patient.allergies || 'NKDA'}` in the handoff prompt and line 68 is `ALLERGIES: ${patient.allergies || 'NKDA'}` in the detailed prompt; the same substitution exists at PatientRiskStratification.jsx:76, AIPatientAnalyzer.jsx:42 and AIPatientHistorySummary.jsx:87 (grep confirmed all five). `allergies` is a plain optional string on the entity (base44/entities/Patient.jsonc:257-259, `\"type\": \"string\"`, no default, not required), and at least one creation path deliberately leaves it empty: DocumentToTriageMapper.jsx:99-102 sets `allergies: extractedData.clinical?.allergies || \"\"` with the comment \"Never synthesize a clinical negative ... Charting 'NKDA' for a patient whose allergies simply were not extracted is an affirmative safety falsehood.\" So '' reaches this prompt, the model is told NKDA as fact, and the output is copied to clipboard/downloaded as a nurse-to-nurse handoff (lines 164-185). No guard, default, or caller-side validation prevents it; nothing in the repo populates allergies with a real value before summary generation.",
   "corrected_severity": "high",
   "corrected_fix": "Replace the fallback with an explicit unknown rather than a negative. In src/components/patient/PatientSummaryGenerator.jsx line 120: `Allergies: ${patient.allergies || 'Not documented \u2014 allergy status unknown, verify before administering medication'}` and the identical change at line 68 (`ALLERGIES: ...`). Apply the same one-line substitution at PatientRiskStratification.jsx:76, AIPatientAnalyzer.jsx:42 and AIPatientHistorySummary.jsx:87. No other prompt text or schema changes."
  }
 ],
 "src/components/patient/patientTimeline.js": [
  {
   "file": "src/components/patient/patientTimeline.js",
   "symbol": "buildPatientTimeline",
   "summary": "Visit timeline entries link to /PatientDetails?id=<visit.id>, passing a Visit id where the page reads a Patient id, so clicking a visit row lands on 'Patient not found'.",
   "real": true,
   "reason": "Could not refute. Line 51 is as quoted and appends the record's own id for every type; EVENT_CONFIG.visit.route is '/PatientDetails' (line 2), so a visit `{id:'v1', patient_id:'p1'}` yields '/PatientDetails?id=v1'. PatientDetails.jsx:67 reads `searchParams.get('id') || searchParams.get('patientId')` as the patient id and renders the 'Patient not found' state at line 187-188 (which even prints 'Patient ID: v1'). The route is actually followed: PatientTimelineStrip.jsx wraps each event in `<Link to={event.route}>`, and it is rendered on every patient chart via ClinicalEventsTimeline.jsx:110-116 (PatientDetails.jsx:310), which passes visits either from the parent or its own `Visit.filter({patient_id})` query. The colocated test only asserts the document route ('/DocumentHub?id=d1', patientTimeline.test.js:13) \u2014 no test exercises the visit route. Other types are benign (DocumentHub/IncidentReview take their own record id; Dashboard/ReferralIntake ignore it); visit is the only type whose id is the wrong entity for its target page.",
   "corrected_severity": "medium",
   "corrected_fix": "In src/components/patient/patientTimeline.js, give the config an explicit id source and use it when building the route. Add `idField: 'patient_id'` to the visit entry (line 2) \u2014 every other entry keeps the default. Then change line 51 to:\n\n  const routeId = config.idField ? record[config.idField] : record.id;\n  ...\n  route: routeId ? `${config.route}?id=${encodeURIComponent(routeId)}` : config.route,\n\nLeave `source_id: record.id` unchanged so the event still identifies the visit. Optionally append `&visitId=${record.id}` for the visit case; add a test asserting the visit route is '/PatientDetails?id=p1'."
  }
 ],
 "src/components/referral/faceToFaceValidator.js": [
  {
   "file": "src/components/referral/faceToFaceValidator.js",
   "symbol": "parseCredential (credentialLike)",
   "summary": "`credentialLike` uses `undotted.indexOf(t)` to locate a token, finding the first substring occurrence anywhere in the string rather than the token's own offset, so the post-comma credential rule mis-fires.",
   "real": true,
   "reason": "Code confirmed verbatim at faceToFaceValidator.js:72-73. The `fromName: true` path is genuinely reachable: validateFaceToFace (line 113-116) falls back to `parseCredential(encounter.practitioner_name, {fromName:true})` whenever `practitioner_credential`/`practitioner_type` are empty, and referralToF2FInput maps `f2f.practitioner_type` straight from LLM extraction (referralExtraction.js:540), which is frequently absent while `practitioner_name` carries the signature. I executed the real module: parseCredential('Papadopoulos, Pa',{fromName:true}) and parseCredential('Patel, Pa',...), parseCredential('Doyle, Do',...), parseCredential('Douglas, Do',...) all return {credential:null, eligible:null}, while the equivalent non-colliding 'Nguyen, Do' and 'Smith, Md' correctly return DO/MD eligible. The trigger is a preceding word whose first letters match the title-cased credential with identical capitalization (indexOf is case-sensitive, so 'Madison, Md' is unaffected \u2014 the claim's generalization is slightly broader than reality, but the concrete cited case is real and common surnames Patel/Park/Palmer with 'Pa', Doyle/Douglas/Donovan with 'Do' hit it). Running validateFaceToFace end-to-end on an otherwise fully compliant encounter with practitioner_name 'Papadopoulos, Pa' yields status 'needs_review' (eligible=null \u21d2 anyUnknown), which referralFollowUpEngine.js:170-185 turns into the high-severity `f2f_invalid` \"Face-to-Face encounter needs clarification\" item and an unnecessary provider information request. The full test file (22 tests, all passing) covers 'Nguyen Do', 'Nguyen Tran, DO', 'John Smith, MD', 'Dr. Lee, DO' \u2014 none exercise a mixed-case credential colliding with an earlier word, so no passing test refutes it. One part of the claim IS wrong: the 'mirror case' \"Ali, Md Rahman\" is not an indexOf defect \u2014 correct offset tracking gives the same true result (offset 5 is genuinely preceded by 'Ali, '); that false pass is inherent to the comma heuristic, not to indexOf. The false-negative direction stands on its own.",
   "corrected_severity": "low",
   "corrected_fix": "In src/components/referral/faceToFaceValidator.js, capture each token's real offset while tokenizing and use it instead of indexOf. Replace line 66:\n\n  const rawTokens = undotted.split(/[^A-Za-z-]+/).filter(Boolean);\n\nwith:\n\n  const tokenMatches = [...undotted.matchAll(/[A-Za-z-]+/g)];\n  const rawTokens = tokenMatches.map((m) => m[0]);\n\n(these are equivalent token sets \u2014 the old split delimiter is the complement of [A-Za-z-]+), and replace line 72:\n\n    const idx = undotted.indexOf(t);\n\nwith:\n\n    const idx = tokenMatches[i].index;\n\nLine 73's comma test is unchanged. Nothing else in the file touches rawTokens beyond index-based reads at lines 69, 76 and 80, so behavior for every currently-passing case is preserved."
  }
 ],
 "src/components/referral/ProviderFollowUpForm.jsx": [
  {
   "file": "src/components/referral/ProviderFollowUpForm.jsx",
   "symbol": "downloadPdf",
   "summary": "The provider follow-up PDF filename embeds the patient's name, writing PHI into browser download history and file-system metadata, contrary to the convention this same feature documents twice.",
   "real": true,
   "reason": "Line 71 confirmed verbatim: filename is `referral-follow-up-${(header.patientName || \"patient\").replace(/\\s+/g,\"-\").toLowerCase()}.pdf`. No guard sanitizes or omits the name \u2014 `header.patientName` is the real patient name (it is also rendered into the subtitle on line 73 and into ReferralFollowUp.jsx's batch headings from `r.patient_name`). The cited counter-examples are real and in the same feature: ReferralPDFSummarizer.jsx:188-192 uses `admission_packet_${Date.now()}.pdf` with the explicit comment \"Use a non-identifying filename \u2014 embedding the patient's name would leak PHI into browser download history and stored file metadata\", ReferralTriageAnalyzer.jsx:96-99 uses `triage-${Date.now()}.json` with the same comment, and the batch export of this very form at ReferralFollowUp.jsx:452 uses the static `referral-follow-up-requests-critical.pdf`. So the single-form path is the odd one out within its own module. Not fully refuted: it is a genuine (if minor) PHI-in-metadata exposure on shared intake workstations. Weakening context worth noting to the parent: this is a convention violation rather than a functional defect, and other unrelated areas of the repo do the same thing (DischargeSummaries.jsx:42, DischargeSummaryWorkflow.jsx:197, SmartOASISAssessment.jsx:512), so it is not a unique lapse \u2014 but within referral it contradicts an explicitly documented rule.",
   "corrected_severity": "low",
   "corrected_fix": "In src/components/referral/ProviderFollowUpForm.jsx, change line 71 from\n\n        filename: `referral-follow-up-${(header.patientName || \"patient\").replace(/\\s+/g, \"-\").toLowerCase()}.pdf`,\n\nto\n\n        // Non-identifying filename \u2014 embedding the patient's name would leak PHI\n        // into browser download history and stored file metadata.\n        filename: `referral-follow-up-${Date.now()}.pdf`,\n\nLeave line 73's subtitle untouched: the patient identity belongs inside the document body, and the form is unusable without it."
  }
 ],
 "src/lib/VisualEditAgent.jsx": [
  {
   "file": "src/lib/VisualEditAgent.jsx",
   "symbol": "VisualEditAgent (handleMessage)",
   "summary": "Production build registers a global 'message' listener with the origin check commented out, exposing refresh-page, update-content and visual-edit-mode to any cross-origin frame/opener.",
   "real": true,
   "reason": "Verified: src/lib/VisualEditAgent.jsx:425-427 contains the commented-out origin check exactly as quoted; `handleMessage` dispatches on `event.data.type` with no `event.origin` inspection; `window.addEventListener('message', handleMessage)` is at line 544; `case 'refresh-page': window.location.reload()` and `update-content` -> `element.innerText = content` (line 326) exist; outbound posts use '*' (lines 188, 253, 415, 503, 549). `<VisualEditAgent />` is mounted unconditionally at src/App.jsx:284 with no `import.meta.env.DEV` guard. The existing test src/lib/VisualEditAgent.test.jsx fires MessageEvents but asserts nothing about origin, so it does not refute. Partial refutation of the claim's *specific* data-loss scenario: SmartNoteAssistant autosaves note text to sessionStorage and IndexedDB on every keystroke (src/pages/SmartNoteAssistant.jsx:262-276), so a forced reload would not destroy a Smart Note. The underlying defect \u2014 an unauthenticated cross-origin control channel present in the production clinical app that can reload the page and rewrite displayed clinical text, and that leaks rendered element text outward with '*' \u2014 is still real and reachable via any framing or window.open relationship.",
   "corrected_severity": "medium",
   "corrected_fix": "Two surgical changes. (1) src/lib/VisualEditAgent.jsx: replace lines 426-427 (the commented-out check) with a real guard at the top of `handleMessage`: `if (!ALLOWED_EDITOR_ORIGINS.includes(event.origin)) return;`, where `ALLOWED_EDITOR_ORIGINS` is a module-level array of the Base44 editor/preview origins (or derived from an env var); and replace the '*' targetOrigin on lines 188, 253, 415, 503 and 549 with that same validated origin. (2) src/App.jsx:284: change `<VisualEditAgent />` to `{import.meta.env.DEV && <VisualEditAgent />}` so the listener does not exist in the production clinical build. Update src/lib/VisualEditAgent.test.jsx's `enableVisualEditMode` helper to supply a matching `origin` on the MessageEvent."
  }
 ],
 "src/lib/app-params.js": [
  {
   "file": "src/lib/app-params.js",
   "symbol": "getAppParamValue / getAppParams",
   "summary": "app_id and functions_version are taken verbatim from the query string and persisted to localStorage permanently, overriding the build-time default with no in-app reset.",
   "real": true,
   "reason": "Verified. `getAppParamValue` (lines 98-131) persists any non-empty sanitized URL value via `setStoredItem(storageKey, searchParam)` at line 117, and on later loads returns `storedValue` (line 122) before ever reaching `sanitizedDefault` (line 125). `appId` is fetched at line 148 with no `sanitize` override and no `acceptUrlValue` gate, and `functionsVersion` at line 162 with neither \u2014 in contrast to `server_url` (line 149, gated by `sanitizeServerUrl`/`isTrustedBackendHost`) and `access_token` (line 161, gated by `trustedTokenReferrer`). I grepped for any reset path: nothing in src/ ever removes `base44_app_id` or `base44_functions_version`; the self-heal at lines 140-146 covers only `base44_clear_access_token`, and the PHI purge key registry (src/lib/offlineKeys.js) lists `base44_from_url` but not `base44_app_id`. So `https://<host>/Dashboard?app_id=bogus` permanently pins that device to a wrong appId, and every SDK call fails, with no in-app way to recover (the user must clear site data). Concrete and reachable from a single crafted or stale link.",
   "corrected_severity": "medium",
   "corrected_fix": "In src/lib/app-params.js, add a build-time gate mirroring the existing `acceptUrlValue` pattern. Above `getAppParams`, add: `const envAppId = import.meta.env.VITE_BASE44_APP_ID; const acceptAppId = () => !envAppId || new URLSearchParams(window.location.search).get('app_id')?.trim() === envAppId;`. Then change line 148 to `getAppParamValue('app_id', { defaultValue: envAppId, acceptUrlValue: acceptAppId })` and line 162 to `getAppParamValue('functions_version', { acceptUrlValue: acceptAppId })` (or simply stop persisting functions_version by reading it straight from the URL, the way `clear_access_token` is read at lines 140-146). Additionally, in the `if (!isNode)` block at line 140, add a self-heal for already-affected devices: `const stored = storage.getItem('base44_app_id'); if (envAppId && stored && stored !== envAppId) { storage.removeItem('base44_app_id'); storage.removeItem('base44_functions_version'); }`. Leave `server_url` and `access_token` handling untouched."
  }
 ],
 "src/components/telehealth/SessionDocumentation.jsx": [
  {
   "file": "src/components/telehealth/SessionDocumentation.jsx",
   "symbol": "SessionDocumentation (formData.vitals_captured default)",
   "summary": "Post-visit documentation seeds vitals_captured from a stale join-time session snapshot and the save writes it verbatim, erasing vitals RealtimeVitalMonitor recorded mid-call.",
   "real": true,
   "reason": "Verified end to end and could not refute. src/pages/Telehealth.jsx:114-119 handleJoin does setActiveSession(session) with the pre-call row and never refreshes it (the ['telehealth-sessions'] query result is never re-selected by id into activeSession). RealtimeVitalMonitor.jsx:128-136 writes vitals_captured directly via base44.entities.TelehealthSession.update, so the recorded values exist only on the server row. On end-of-call Telehealth.jsx:240-246 renders SessionDocumentation with initialData={activeSession}, whose vitals_captured is undefined, so line 14 falls back to the all-empty-string literal; handleSaveDocumentation (Telehealth.jsx:147-152) passes docData straight through as the update payload, replacing the persisted object. No guard exists: TelehealthSession.jsonc declares vitals_captured as a bare {\"type\":\"object\"} with no property types, so empty strings are accepted silently \u2014 no validation error, no toast. No test touches SessionDocumentation or vitals_captured (grep over *.test.js/*.spec.js finds only ISO scheduled_at fixtures in base44/functions/telnyxContract.test.js). Downstream this also corrupts compliance checks that only count Object.keys(vital_signs).length (src/components/compliance/GranularComplianceGapAnalyzer.jsx:51).",
   "corrected_severity": "high",
   "corrected_fix": "Merge instead of clobber, and read fresh. In src/pages/Telehealth.jsx replace the body of handleSaveDocumentation (lines 147-157) with a version that re-reads the row and prunes blanks:\n\n  const handleSaveDocumentation = async (docData) => {\n    if (!activeSession) return;\n    const fresh = (await base44.entities.TelehealthSession.filter({ id: activeSession.id }))?.[0] || activeSession;\n    const entered = Object.fromEntries(\n      Object.entries(docData.vitals_captured || {}).filter(([, v]) => v !== '' && v != null)\n    );\n    await updateSession.mutateAsync({\n      id: activeSession.id,\n      data: { ...docData, vitals_captured: { ...(fresh.vitals_captured || {}), ...entered } }\n    });\n    setActiveSession(null);\n    setShowDocumentation(false);\n    endingRef.current = false;\n  };\n\nAlso pass the fresh row to the form so the nurse sees the recorded vitals: at line 245 use initialData={sessions.find(s => s.id === activeSession.id) || activeSession}. Apply the same blank-pruning merge in PatientTelehealthPanel.jsx:154-157, which spreads ...docData into the session update the same way. No other behavior changes."
  }
 ],
 "src/components/telehealth/PatientTelehealthPanel.jsx": [
  {
   "file": "src/components/telehealth/PatientTelehealthPanel.jsx",
   "symbol": "saveDocumentation",
   "summary": "docData.vitals_captured holds raw input strings (and '' for untouched fields) but is written directly into Visit.vital_signs, whose schema types every vital as number.",
   "real": true,
   "reason": "Confirmed at src/components/telehealth/PatientTelehealthPanel.jsx:150 (`vital_signs: docData.vitals_captured,`) inside createVisitMutation -> Visit.create. SessionDocumentation.jsx:33-41 stores e.target.value verbatim (an <Input type=\"number\"> still yields a string) and lines 14-21 default every untouched field to ''. base44/entities/Visit.jsonc:60-88 declares temperature/heart_rate/blood_pressure_*/oxygen_saturation/pain_level as \"type\": \"number\". No coercion or pruning anywhere on this path, and no test covers it. The codebase already documents this exact hazard and guards it on the other Visit.create path \u2014 src/components/mobile/OfflineTaskManager.jsx:70-88 builds `coercedVitals` with the comment \"The Visit schema types every vital_signs.* as a NUMBER ... coerce to numbers and drop blanks \u2014 otherwise the create is rejected ... or string/empty vitals corrupt downstream numeric trend/bounds logic.\" That precedent makes the telehealth path an unguarded outlier, not an accepted convention. If the create is not rejected, the strings persist: src/components/admin/ReportsCenter.jsx:562-575 does `(avg || 0) + v.vital_signs.blood_pressure_systolic`, which string-concatenates a \"120\" into garbage averages, and the six '' keys make GranularComplianceGapAnalyzer.jsx:51 report vitals as documented when none were. Downgraded from the reporter's framing only in that a rejected create surfaces a visible \"Failed to save\" toast rather than silent loss.",
   "corrected_severity": "medium",
   "corrected_fix": "Mirror the existing OfflineTaskManager guard. In src/components/telehealth/PatientTelehealthPanel.jsx, immediately before the createVisitMutation.mutateAsync call (line 138) insert:\n\n    const coercedVitals = {};\n    for (const [k, v] of Object.entries(docData.vitals_captured || {})) {\n      if (v === '' || v == null) continue;\n      const n = parseFloat(v);\n      if (Number.isFinite(n)) coercedVitals[k] = n;\n    }\n\nand change line 150 to:\n      vital_signs: Object.keys(coercedVitals).length ? coercedVitals : null,\n\nLeave the TelehealthSession update at line 154-157 alone except for the blank-pruning merge described in the first finding."
  },
  {
   "file": "src/components/telehealth/PatientTelehealthPanel.jsx",
   "symbol": "createSession",
   "summary": "The raw datetime-local value (naive wall-clock, no timezone) is stored directly in TelehealthSession.scheduled_at, so the backend's Date.parse resolves it as UTC and the guest join window shifts by the agency's UTC offset.",
   "real": true,
   "reason": "Confirmed: the input at PatientTelehealthPanel.jsx:211 stores e.target.value unchanged and line 180 persists `newSession.scheduled_at || new Date().toISOString()` \u2014 the fallback is ISO but the user-supplied branch is the bare \"YYYY-MM-DDTHH:mm\" string. Same defect at src/pages/Telehealth.jsx:323. base44/functions/createTelehealthToken/entry.ts:131-132 does Date.parse(session.scheduled_at) and compares against GUEST_JOIN_WINDOW_MS (12h, line 24); an offset-less date-time string is parsed in the runtime's local zone, which on the Deno backend is UTC, so an Eastern 09:00 appointment is read as 05:00 ET and the patient's link expires 4-5 hours early. Nothing normalizes it in between: the schema declares only \"format\": \"date-time\" (no coercion in this repo), and the only tests touching scheduled_at (base44/functions/telnyxContract.test.js:516, 563) feed new Date().toISOString(), so this input shape is untested. The codebase's own convention converts first \u2014 DutyStatusCard.jsx fromLocalInput and ScheduleSendDialog both call toISOString() \u2014 making these two sites the outliers. Symptom is silent because the browser-side display (`new Date(session.scheduled_at).toLocaleString()`, line 246) parses the naive string as local and shows the correct time.",
   "corrected_severity": "medium",
   "corrected_fix": "Convert to a real instant at write time. src/components/telehealth/PatientTelehealthPanel.jsx line 180 becomes:\n\n      scheduled_at: newSession.scheduled_at && !Number.isNaN(new Date(newSession.scheduled_at).getTime())\n        ? new Date(newSession.scheduled_at).toISOString()\n        : new Date().toISOString(),\n\nand src/pages/Telehealth.jsx line 323 gets the identical change against form.scheduled_at. new Date(\"2026-08-10T09:00\") in the browser resolves in the nurse's local zone, which is the intended wall-clock meaning, so this only fixes the serialization. No other behavior changes; the display at line 246 keeps working since toLocaleString() renders an ISO instant in local time."
  }
 ],
 "src/components/voice/phoneUtils.js": [
  {
   "file": "src/components/voice/phoneUtils.js",
   "symbol": "normalizeE164",
   "summary": "The bare 10-digit NANP branch runs before the already-+-prefixed branch, so an explicitly international 10-digit E.164 number is rewritten as a US +1 number for a different subscriber.",
   "real": true,
   "reason": "Code confirmed verbatim at phoneUtils.js:12-18 (and mirrored at src/components/integrations/telnyx/telnyxUtils.js:23-28). normalizeE164(\"+49 89 123456\") -> digits \"4989123456\" (length 10) -> returns \"+14989123456\", discarding the caller's explicit + and the country code. The existing tests do not cover this: phoneUtils.test.js only asserts bare 10/11-digit US forms and 12-digit \"+44 20 7946 0958\", plus the length/leading-zero rejections \u2014 nothing with exactly 10 digits after a +. So no passing test pins the current behavior. Consequence is real: costControls.js:29 matches /^\\+1\\d{10}$/ and reports allowed:true, so the international block (allow_international) is bypassed and a fax/SMS goes to an unrelated NANP number \u2014 the misdirected-PHI vector FaxAddressBook.jsx:166-168 explicitly says it is guarding against. Severity reduced from the reporter's medium because reachability is narrow: it requires an operator or CSV to supply a +-prefixed number whose total digit count is exactly 10, which is uncommon for a US home-health agency's fax book (most international numbers are 11-15 digits and already normalize correctly).",
   "corrected_severity": "low",
   "corrected_fix": "Handle the explicit + prefix first and never fall through to the NANP assumptions for it. Replace src/components/voice/phoneUtils.js lines 13-19 with:\n\n  if (String(raw).trim().startsWith(\"+\")) {\n    // Already international: bound to a valid E.164 length (8-15 digits) and reject\n    // a leading 0 (no country code starts with 0).\n    return (digits.length >= 8 && digits.length <= 15 && digits[0] !== \"0\") ? `+${digits}` : null;\n  }\n  if (digits.length === 10) return `+1${digits}`;\n  if (digits.length === 11 && digits.startsWith(\"1\")) return `+${digits}`;\n  return null;\n\nThe early return null inside the + branch is required \u2014 without it \"+0155550100\" (10 digits, leading zero) would fall through to the 10-digit branch and become \"+10155550100\". This keeps every existing assertion green (\"+12155550100\", \"+442079460958\", \"+123456789012345\" pass; \"+1234567\", \"+02155550100\", \"+1234567890123456789\" still null). Apply the identical reordering to src/components/integrations/telnyx/telnyxUtils.js:23-28 so the inline-parity guard stays satisfied."
  }
 ],
 "src/components/fax/EnhancedFaxHistory.jsx": [
  {
   "file": "src/components/fax/EnhancedFaxHistory.jsx",
   "symbol": "EnhancedFaxHistory (retry button gate)",
   "summary": "The manual Retry button is gated on a hardcoded retry_count < 3 instead of the admin-configured FaxRetryConfig.max_retries the backend enforces.",
   "real": true,
   "reason": "Confirmed verbatim at src/components/fax/EnhancedFaxHistory.jsx:181 and the backend does read config: base44/functions/retryFailedFax/entry.ts:118-127 loads FaxRetryConfig and uses `maxRetries = Number.isFinite(cfgMax) && cfgMax >= 0 ? cfgMax : 3`, with the comment \"Honor the admin-configured retry budget ... so a manual retry uses the same limit as the auto-retry cron, instead of a separate hardcoded value\" \u2014 the UI was simply not updated to match. src/components/fax/faxRetry.js exports faxRetryConfig() as the shared normalizer, and EnhancedFaxHistory imports none of it. The mismatch cuts both ways: with max_retries=5 the button vanishes at retry_count 3 while two attempts remain allowed, and with max_retries=2 the button is offered at retry_count 2 and the backend answers HTTP 400. Severity kept at low: this is a manual backstop \u2014 when auto_retry_enabled the cron still consumes the full configured budget, so the document is not necessarily undelivered, and the nurse retains the fallback of re-sending the fax.",
   "corrected_severity": "low",
   "corrected_fix": "Read the config in the component and gate on it. In src/components/fax/EnhancedFaxHistory.jsx add `import { faxRetryConfig } from \"@/components/fax/faxRetry\";` alongside the existing imports, then after the faxLogs query (line 25) add:\n\n  const { data: retryCfgRows = [] } = useQuery({\n    queryKey: ['fax-retry-config'],\n    queryFn: () => base44.entities.FaxRetryConfig.list('-created_date', 1),\n    initialData: []\n  });\n  const maxRetries = faxRetryConfig(retryCfgRows[0]).maxRetries;\n\nand change line 181 to:\n                      {log.status === 'failed' && (Number(log.retry_count) || 0) < maxRetries && (\n\nfaxRetryConfig already defaults maxRetries to 3 when the row is absent, so behavior is unchanged for agencies with no config row."
  }
 ],
 "base44/functions/generateComprehensiveOASISReport/entry.ts": [
  {
   "file": "base44/functions/generateComprehensiveOASISReport/entry.ts",
   "symbol": "Deno.serve handler \u2014 Overall Scores (line 125) and Revenue Tips section (line 211)",
   "summary": "The server-side financial gate is applied only to the dollar-figure blocks; revenue_optimization_score and the entire 'REVENUE OPTIMIZATION OPPORTUNITIES' section render for any authenticated user, unlike every sibling handler.",
   "real": true,
   "reason": "Confirmed verbatim. `canViewFinancials(user)` is defined at line 9 and applied at line 144 (case-mix payment) and line 156 (revenue analysis), but line 125 (`addKeyValue('Revenue Optimization', ...revenue_optimization_score...)`) and the whole block at lines 211-227 (category, potential_impact, opportunity, specific_action for up to 10 tips) are ungated. The sibling generateOASISReportPDF/entry.ts hoists `const allowFinancials = canViewFinancials(user)` at line 25 and gates the exact same two things (score box via `...(allowFinancials ? [...] : [])`, revenue tips via `if (allowFinancials && analysisResults.revenue_tips?.length > 0)`) \u2014 and it takes analysisResults from the request body too, so 'the data is client-supplied' does not excuse the gap; the repo's policy is explicitly to gate rendering regardless of source. processOASISBatch/entry.ts:26-32 403s the whole endpoint for non-financial users with the comment 'generated PDFs embed revenue_optimization_score / revenue_tips ... Enforce it server-side so the endpoint can't be called directly to read that data', and listOASISUploads strips these keys with FINANCIAL_KEY = /revenue|payment|reimburs/i. This file's own header comment (line 8) states 'clinical staff (nurses) must never receive dollar figures, even by calling this endpoint directly'. No guard, early return, or test refutes it. I discount the severity from the claim's framing only because analysisResults is caller-supplied, so no server-held record is disclosed to a user who lacks it \u2014 the defect is a real, isolated break in a policy the codebase enforces everywhere else, producing an exportable/shareable PDF containing financial content for non-financial staff.",
   "corrected_severity": "medium",
   "corrected_fix": "In generateComprehensiveOASISReport/entry.ts, hoist the gate next to the auth check (after line 25, mirroring generateOASISReportPDF:25): `const allowFinancials = canViewFinancials(user);`. Then: (1) change line 125 to `if (allowFinancials) addKeyValue('Revenue Optimization', analysisResults.revenue_optimization_score !== undefined ? `${analysisResults.revenue_optimization_score}%` : 'N/A');`; (2) change line 211 to `if (allowFinancials && analysisResults.revenue_tips?.length > 0) {`. Optionally replace the two existing `canViewFinancials(user)` calls at lines 144 and 156 with `allowFinancials` for consistency. No other behavior changes."
  }
 ],
 "base44/functions/deduplicatePatients/entry.ts": [
  {
   "file": "base44/functions/deduplicatePatients/entry.ts",
   "symbol": "Deno.serve handler \u2014 resultsWithConfidence avgScore reduce",
   "summary": "A removed record whose match_score is deliberately null (set when the removed record was the group's primary) is coerced to 0 by `sum + r.match_score`, dragging the group average down and labelling verified merges as 'Low' confidence at 0%.",
   "real": true,
   "reason": "Both code sites confirmed verbatim (lines 1186-1189 and 1259-1262). The scenario is reachable: Phase 1 (lines 1093-1120) builds an MRN group with `primary = unprocessed[0]` and every other member scored 100, and `primary` itself is never added to `group.duplicates`. The survivor re-selection at lines 1166-1177 sorts the whole group (`[group.primary, ...duplicates]`) by active-status, then completenessScore, then recency \u2014 so whenever the primary is non-active, less complete, or older than a duplicate, the primary lands in `toRemove` and line 1189's `.find(...)?.score ?? null` yields null. `0 + null` evaluates to 0 in JS, so a two-record MRN-verified merge reports avgScore 0 -> confidence 'Low', average_match_score 0; a mixed group is deflated proportionally. This is user-visible, not internal: src/components/patient/DuplicateScanner.jsx lines 601-607 renders `{detail.confidence} Confidence ({detail.average_match_score}%)` with a red badge for 'Low', and it renders for the dry-run preview (confirm=false still populates removedFromGroup at line 1194 and detailsArray), i.e. it feeds the admin's decision to approve or skip a chart merge. The null was clearly intentional (see the comment at 1186-1188), which makes the unguarded reduce the defect. No test covers this path (DuplicateScanner.spec.jsx exercises the client-side scanner, not this reduce).",
   "corrected_severity": "medium",
   "corrected_fix": "Replace lines 1259-1272 in deduplicatePatients/entry.ts so nulls are excluded rather than folded in as zeros:\n```\n    const resultsWithConfidence = detailsArray.map((detail) => {\n      const scored = detail.removed.filter((r) => typeof r.match_score === 'number');\n      if (scored.length === 0) {\n        // Nothing in this group carries a per-patient score (e.g. only the\n        // group's primary was removed) \u2014 report no confidence rather than 0%.\n        return { ...detail, confidence: null, average_match_score: null };\n      }\n      const avgScore = scored.reduce((sum, r) => sum + r.match_score, 0) / scored.length;\n      let confidence = 'High';\n      if (avgScore < 70) confidence = 'Medium';\n      if (avgScore < 50) confidence = 'Low';\n      return { ...detail, confidence, average_match_score: Math.round(avgScore) };\n    });\n```\nThe existing `{detail.confidence && (...)}` guard at DuplicateScanner.jsx:601 already hides the badge when confidence is null, so no frontend change is required."
  }
 ],
 "base44/functions/embedAnnotationsToPDF/entry.ts": [
  {
   "file": "base44/functions/embedAnnotationsToPDF/entry.ts",
   "symbol": "Deno.serve handler \u2014 UserActivity details.total_annotations reduce",
   "summary": "The total_annotations reduce dereferences .length without the Array.isArray guard used 13 lines earlier, so a non-array page entry throws after the signed PDF has already been uploaded \u2014 500 to the caller, no audit row, orphan signed PDF in storage.",
   "real": true,
   "reason": "Confirmed verbatim: line 155 guards with `Array.isArray(pageAnnots) ? ... : 0` while line 167 does the bare `sum + arr.length` over the same `Object.values(annotations)`. The path is reachable: the only input validation is `if (!pdf_url || !annotations)` at line 62 (truthiness of the container only, no shape check), and the embed loop at line 88 explicitly tolerates falsy page values via `|| !pageAnnotations || pageAnnotations.length === 0 -> continue`. So `{\"1\": [ ...sigs ], \"2\": null}` embeds fine, saves at 142, uploads at 151, computes signatureCount at 154-156 without error, then throws TypeError on `null.length` at 167 \u2014 after the upload has succeeded. The outer catch (line 181) swallows it into a generic 500, so UserActivity.create never runs and `uploadResult.file_url` is never returned: the file exists in storage with no reference and no audit trail. I could not refute reachability \u2014 there is no in-repo caller to constrain the payload (grep found only base44/functions/activeUserGuardContract.test.js:80; src/components/documents/PDFEditor.jsx calls saveAnnotatedPDF and src/components/fax/PDFAnnotator.jsx flattens client-side), which means this is an open authenticated HTTP endpoint whose payload shape is entirely caller-controlled, and no test exercises it. Severity is capped at low because triggering it requires a malformed payload no current client sends, and the blast radius is the caller's own request plus a stray storage object.",
   "corrected_severity": "low",
   "corrected_fix": "Two-line change in embedAnnotationsToPDF/entry.ts. (1) Mirror the existing guard at line 167: `total_annotations: Object.values(annotations).reduce((sum, arr) => sum + (Array.isArray(arr) ? arr.length : 0), 0),`. (2) Make the audit write non-fatal so no future throw can discard an already-uploaded PDF \u2014 wrap the `await base44.asServiceRole.entities.UserActivity.create({...})` block at lines 159-172 in `try { ... } catch (logErr) { console.error('Failed to log document_signed activity:', logErr.message); }`, leaving the success Response at 174-179 untouched. Do not change the loop at line 88 or the signatureCount reduce."
  }
 ],
 "base44/functions/processOASISBatch/entry.ts": [
  {
   "file": "base44/functions/processOASISBatch/entry.ts",
   "symbol": "Deno.serve handler \u2014 zip.file(safeName)",
   "summary": "Sanitized filenames are not deduplicated, so documents that normalize to the same safeName overwrite each other in the ZIP while both are reported as successes.",
   "real": true,
   "reason": "Confirmed and reachable. entry.ts:136 computes safeName = fileName.replace(/[^a-zA-Z0-9_-]/g,'_').substring(0,50) and entry.ts:169 writes zip.file(`${r.safeName}_Analysis.pdf`, r.pdfBytes); JSZip keys entries by name in its files map, so a repeated name replaces the earlier entry rather than adding a second one. Meanwhile results.push at :170 and successCount at :183 still count both, so the response claims N successes while the ZIP contains fewer members. The caller makes collisions genuinely reachable: /home/user/CareMetric-Pennsync/src/components/oasis/BatchOASISAnalyzer.jsx:72 does setFiles(prev => [...prev, ...fileObjects]) \u2014 files accumulate across repeated picker selections with no name dedup \u2014 and fileNames are pushed verbatim (line ~134) and passed straight to processOASISBatch (line 173). The same component already documents that duplicate names occur in practice: the result-matching comment at line ~183 says 'two files sharing a name each claim their own result instead of both overwriting the first'. Two identically-named PDFs (or two long OASIS exports sharing their first 50 sanitized chars, or 'Report (1).pdf' vs 'Report [1].pdf') therefore produce one ZIP entry for two analyses. No test covers processOASISBatch (no processOASISBatch test file exists under base44/functions/*.test.js), so nothing refutes it. Impact is bounded \u2014 both analyses are still returned in results and rendered in the UI, so only the downloadable PDF archive silently loses a document's compliance/accuracy findings, and re-running recovers it \u2014 which keeps this at low severity rather than higher.",
   "corrected_severity": "low",
   "corrected_fix": "Dedupe at the ZIP-write site so non-colliding names keep their current filenames. In base44/functions/processOASISBatch/entry.ts: after `const zip = new JSZip();` (line 41) add `const usedZipNames = new Set();`. Then replace line 169 `zip.file(`${r.safeName}_Analysis.pdf`, r.pdfBytes);` with:\n  let entryName = `${r.safeName}_Analysis.pdf`;\n  for (let n = 2; usedZipNames.has(entryName); n++) entryName = `${r.safeName}_Analysis_${n}.pdf`;\n  usedZipNames.add(entryName);\n  zip.file(entryName, r.pdfBytes);\nThis is preferable to the claim's suggested index-prefix, which would rename every entry and change the download for all existing users; the loop only alters names that would otherwise collide. Nothing else changes \u2014 results.push, successCount and the response shape stay as-is."
  }
 ],
 "base44/functions/generateLearningTranscriptPDF/entry.ts": [
  {
   "file": "base44/functions/generateLearningTranscriptPDF/entry.ts",
   "symbol": "Deno.serve handler",
   "summary": "employeeId is never validated, so an omitted value produces unscoped User and TrainingCertificate queries",
   "real": true,
   "reason": "Mechanism verified end to end. Line 80's guard short-circuits to false for account_type 'agency_admin'/'super_admin', so a body with no employeeId is authorized. The SDK serializes filters with JSON.stringify (node_modules/@base44/sdk/dist/modules/entities.js:71-73: `q: JSON.stringify(query)`), and JSON.stringify drops undefined-valued keys \u2014 so line 85 becomes `q={}` (every user, employees[0] arbitrary) and line 103 becomes `q={\"revoked\":false}` (every clinician's certificates, up to 5000). Nothing downstream re-checks: the agency_name check at line 91 compares the arbitrary employees[0], and for super_admin there is no check at all. No caller in the repo omits employeeId (LearningCenter.jsx:365 always sends user.email and early-returns on `!user?.email`), and no test covers this path, so it is only reachable by a hand-crafted request from an already-admin session \u2014 which is why I score the impact down rather than refuting it. Real impact is a garbage/over-broad transcript (course rows only, no per-row identities) and, for an agency_admin whose employees[0] happens to land in their own agency, cross-agency certificate rows; nurses are still correctly 403'd.",
   "corrected_severity": "low",
   "corrected_fix": "Insert immediately after line 77 (before the authorization branch at line 80):\n`if (!employeeId || typeof employeeId !== 'string') { return Response.json({ error: 'employeeId is required' }, { status: 400 }); }`\nNothing else changes \u2014 every existing caller already sends a string employeeId, so behavior for real traffic is identical."
  }
 ],
 "base44/functions/manageSmsConsent/entry.ts": [
  {
   "file": "base44/functions/manageSmsConsent/entry.ts",
   "symbol": "Deno.serve handler ('list' action)",
   "summary": "Consent totals count ledger events over the newest 500 rows instead of current state per phone number",
   "real": true,
   "reason": "Premises check out. SmsConsent is append-only (handleTelnyxStatusWebhook/entry.ts:501 creates a row per STOP/START) and every send gate resolves state from the single newest row per number (sendSms:524, dispatchScheduledSms:609, scheduleSms:66, redriveFailedSms:498, sendTestSms:136, all `.filter({ phone_e164 }, '-captured_at', 1)`). The loop at lines 58-64 tallies raw rows with no dedupe, so one number that texted STOP then START adds 1 to opted_out and 1 to opted_in even though its live state is opted_in \u2014 and line 55 caps the scan at 500 rows. No test covers these totals (only the client-side summarizeConsent helper is tested, and that helper is a display fallback for the rows already on screen). The numbers are surfaced as headline compliance badges in src/components/admin/ConsentLedgerPanel.jsx ('N opted in / N opted out'), where the code comment explicitly claims they are 'the server-computed totals over the whole ledger' \u2014 which they are not. Concrete failure: one patient texts STOP then START; the TCPA panel shows '1 opted out' when zero numbers are currently opted out. Downgraded to low because no message is sent or blocked incorrectly \u2014 the send gates are right; only the dashboard figure misleads.",
   "corrected_severity": "low",
   "corrected_fix": "Replace the tally loop at lines 58-64 with a latest-per-number collapse (the list is already '-captured_at' ordered, so the first row seen per key is the newest):\n```\nconst totals = { opted_in: 0, opted_out: 0, unknown: 0 };\nconst latestByPhone = new Map();\nfor (const r of list) {\n  const key = r?.phone_e164 || '';\n  if (!key || latestByPhone.has(key)) continue;\n  latestByPhone.set(key, r);\n}\nfor (const r of latestByPhone.values()) {\n  const s = r?.consent_status;\n  if (s === 'opted_in') totals.opted_in += 1;\n  else if (s === 'opted_out') totals.opted_out += 1;\n  else totals.unknown += 1;\n}\n```\nLeave the `recent` slice (lines 66-80) untouched \u2014 it is meant to be an event list. Separately, either raise the line 55 page size or return a `totals_truncated: list.length >= 500` flag and fix the now-inaccurate 'over the whole ledger' comment in ConsentLedgerPanel.jsx."
  }
 ],
 "base44/functions/handleTelnyxStatusWebhook/entry.ts": [
  {
   "file": "base44/functions/handleTelnyxStatusWebhook/entry.ts",
   "symbol": "startRingdown",
   "summary": "When every ringdown transfer command is rejected, the caller is apologised to and hung up but the CallLog is never marked 'failed', so the trailing call.hangup writes 'completed' and the missed call vanishes from the callback queue.",
   "real": true,
   "reason": "Quoted code exists verbatim at entry.ts:1102-1103. Verified the whole chain: (a) startRingdown (line 1089) takes no base44 client and does no CallLog write on the all-rejected fallback; (b) the fallback is reachable \u2014 buildRingdown (lines 809-826) pushes raw stored strings with NO normalizeE164, so a malformed personal_cell_e164/main office number produces a 422 for every target (the existing test at telnyxContract.test.js:750 uses exactly such a value, '724-465', and only asserts the 2-target retry, never the all-rejected case); (c) the a_leg's client_state at that point is the {t:'inbound_ivr'} set by the answer command at line 960, never {t:'ringdown', a_leg}, so the exhaustion branch at 968-989 (which does mark 'failed') cannot fire; (d) execution therefore falls through to the generic updater at 1056-1078 where mapCallStatus('call.hangup')==='completed' (line 66) and CALL_RANK.completed(3) > CALL_RANK.ringing(1) (line 76), so the row created by logInboundCall with status 'ringing' is overwritten to 'completed'; (e) impact confirmed downstream \u2014 src/components/voice/callbackQueue.js:37 counts a call as missed only when status is 'no_answer' or 'failed'. No guard, caller-side validation, or test refutes it; it is the exact failure the comment at lines 974-979 says must not happen.",
   "corrected_severity": "medium",
   "corrected_fix": "Thread the client through and reuse the exhaustion-branch treatment. 1) Change line 1089 to `async function startRingdown(base44, apiKey, aLegId, targets, callerId, idx = 0)`. 2) Update the four call sites to pass base44: line 972 `startRingdown(base44, apiKey, state.a_leg, ...)`, line 997 `startRingdown(base44, apiKey, callControlId, state.targets || [], ...)`, line 1020 `startRingdown(base44, apiKey, callControlId, route.targets || [], ...)`, line 1109 `startRingdown(base44, apiKey, callControlId, targets || ..., callerId, 0)` (continueAfterGreeting already receives base44). 3) Immediately before line 1102 insert the same write as lines 980-987: `const rows = await base44.asServiceRole.entities.CallLog.filter({ provider_call_id: aLegId }, '-created_date', 1).catch(() => []); if (rows.length && rows[0].status !== 'failed') { await base44.asServiceRole.entities.CallLog.update(rows[0].id, { status: 'failed', failure_reason: 'No answer \u2014 all on-call targets were unavailable' }).catch(() => {}); }`. CALL_RANK.failed (4) already outranks 'completed', so the trailing hangup cannot regress it. No other behaviour changes."
  },
  {
   "file": "base44/functions/handleTelnyxStatusWebhook/entry.ts",
   "symbol": "handleCallEvent (call.answered recovery path)",
   "summary": "The 'call.initiated was lost' recovery branch re-derives the route and drives the call but never calls logInboundCall, so no CallLog row exists and every downstream voicemail/transcript/status write for that call is silently discarded.",
   "real": true,
   "reason": "Quoted code exists at entry.ts:1015-1030. The primary branch calls `await logInboundCall(...)` at line 958; the recovery branch has no such call and returns early at 1021/1030, so it never reaches the generic updater at 1056 either. Confirmed the downstream consequences: appendVoicemailTranscript (1143-1144) and saveVoicemail (1157-1158) both `filter({provider_call_id})` and `if (!rows.length) return;` \u2014 no row means the recording URL, transcript, has_voicemail flag, and the '\ud83d\udcde New voicemail' Notification at 1170-1174 are all dropped, and the call never appears in call history. Nothing else creates an inbound CallLog row (startMaskedCall is outbound only). The claimed fix is also safe: logInboundCall early-returns when a row for that provider_call_id already exists (lines 910-911), so a late call.initiated cannot double-write. No test exercises this branch (grep for 'inbound_recovered' across the suite returns nothing).",
   "corrected_severity": "high",
   "corrected_fix": "In the recovery branch, mirror line 958. After `const route = await decideInboundRouting(base44, config, workNum);` (line 1018) insert: `await logInboundCall(base44, callControlId, normalizeE164(payload?.from) || payload?.from || '', workNum, route);`. The `workNum` const already exists at line 1017; nothing else changes, and logInboundCall's existing-row check keeps it idempotent against a delayed call.initiated."
  }
 ],
 "base44/functions/onUserSignup/entry.ts": [
  {
   "file": "base44/functions/onUserSignup/entry.ts",
   "symbol": "Deno.serve handler (invitation lookup)",
   "summary": "Invitation expiry is not enforced at the point of use \u2014 onUserSignup and autoApproveInvitedUser select on status:'pending' alone and never read expires_at.",
   "real": true,
   "reason": "Factually confirmed: the filter at onUserSignup/entry.ts:246-248 is `{status:'pending'}` with no expiry test, `expires_at` appears nowhere in that file (the only hit, line 439, is an unrelated otp_expires_at), and autoApproveInvitedUser/entry.ts:176-180 has the same filter with no expiry check before applying `role: invitation.role, is_approved: true` (lines 213-222). So the code claim survives. However the claimed impact is much smaller than 'high': expiry enforcement is deliberately centralised in checkExpiredInvitations (entry.ts:180-212), which flips pending->expired and even fails closed on an unparseable expires_at \u2014 i.e. the design intends 'pending' to mean 'actionable' and the sweep to be the enforcer, the same operational assumption every other cron in this codebase makes (autoApproveInvitedUser itself only works if scheduled). Exploiting the residual window also requires possession of the invitee's own mailbox/platform invite. I am recording it as a real hardening gap (the advertised 7-day window is unenforced between expiry and the next sweep, and indefinitely if the sweep is not scheduled), not a high-severity authorization defect.",
   "corrected_severity": "low",
   "corrected_fix": "Add a point-of-use expiry check that fails closed, mirroring checkExpiredInvitations:192-197. In onUserSignup/entry.ts, after `const invitation = invitations[0];` (line 255) insert: `const expMs = Date.parse(invitation.expires_at || ''); if (!Number.isFinite(expMs) || expMs <= Date.now()) { await base44.asServiceRole.entities.UserInvitation.update(invitation.id, { status: 'expired' }).catch(() => {}); return Response.json({ error: 'This invitation has expired. Ask an administrator to send a new one.' }, { status: 403 }); }`. In autoApproveInvitedUser/entry.ts, at the top of the `for (const invitation of invitations)` loop body (before the User lookup, ~line 194) insert the same parse and, when expired, `await base44.asServiceRole.entities.UserInvitation.update(invitation.id, { status: 'expired' }); skippedCount++; continue;`. Do not touch the resend paths \u2014 they already refresh expires_at."
  }
 ],
 "src/components/admin/ReportsCenter.jsx": [
  {
   "file": "src/components/admin/ReportsCenter.jsx",
   "symbol": "generateClinicalReport \u2014 vital-signs averages",
   "summary": "Average systolic BP and heart rate are divided by the count of visits having any vital_signs object rather than the count of visits that recorded that specific measurement, systematically understating both averages.",
   "real": true,
   "reason": "CONFIRMED at /home/user/CareMetric-Pennsync/src/components/admin/ReportsCenter.jsx:561-575, quoted exactly. The numerators at lines 563-568 are gated on the individual field being truthy, but the divisor at line 571 is `visits.filter(v => v.vital_signs && Object.keys(v.vital_signs).length > 0).length` \u2014 every visit with any vitals key. Partial vitals are not hypothetical: VitalSignsForm (src/components/visit/VitalSignsForm.jsx:29-34) writes each of its seven fields independently and stores `null` for any left blank, so a visit where the nurse recorded only heart rate still contributes a full unit to the denominator for BP \u2014 and a visit where the form was opened and every field cleared contributes to BOTH denominators while contributing to neither numerator (Object.keys on an all-null object is still non-empty). No guard, default, or caller-side filter prevents this: generateClinicalReport is called at line 279 with `filteredVisits` (date-window filtered only). No test file covers ReportsCenter (the admin test files are adminOnboardingChecklist, commsDashboard, consentLedger, csvExport, phoneAnalytics, runUserOffboard, setupStages, telnyxSetup, userOffboarding). The wrong value is exported at line 590 as \"Average Systolic BP\".",
   "corrected_severity": "medium",
   "corrected_fix": "In /home/user/CareMetric-Pennsync/src/components/admin/ReportsCenter.jsx replace lines 560-575 with per-metric sums and counters:\n\n    // Analyze vital signs trends \u2014 each average is divided by the number of\n    // visits that recorded THAT measurement, not by visits with any vitals key.\n    const vitalsTrends = {};\n    let bpSum = 0, bpCount = 0, hrSum = 0, hrCount = 0;\n    visits.forEach(v => {\n      const vs = v.vital_signs;\n      if (!vs) return;\n      if (vs.blood_pressure_systolic) { bpSum += Number(vs.blood_pressure_systolic); bpCount += 1; }\n      if (vs.heart_rate) { hrSum += Number(vs.heart_rate); hrCount += 1; }\n    });\n    if (bpCount > 0) vitalsTrends.avgBPSystolic = Math.round(bpSum / bpCount);\n    if (hrCount > 0) vitalsTrends.avgHeartRate = Math.round(hrSum / hrCount);\n\nThe local `const visitsWithVitals` on line 571 exists only to serve these two divisors and should be deleted with them (do not touch the separate `visitsWithVitals` array used by the compliance report around line 540). Lines 590-591 already print 'N/A' when a metric is absent, so the zero-sample case stays correct and no other behavior changes."
  },
  {
   "file": "src/components/admin/ReportsCenter.jsx",
   "symbol": "generateTrendAnalysisData / generateProductivityReportData \u2014 day labels",
   "summary": "Day buckets are keyed on local calendar days but relabelled by re-parsing the 'yyyy-MM-dd' string with `new Date()`, which parses date-only strings as UTC midnight, so every chart point and CSV row is labelled with the previous day in any negative-UTC-offset timezone.",
   "real": true,
   "reason": "CONFIRMED at both cited sites, quoted exactly: /home/user/CareMetric-Pennsync/src/components/admin/ReportsCenter.jsx:804-809 (generateTrendAnalysisData) and :363-369 (generateProductivityReportData's dailyEnhancements). The key is built locally via `format(subDays(new Date(), i), 'yyyy-MM-dd')` and matched correctly against date-only fields, but the label re-parses that string: `format(new Date(date), 'MM/dd')`. Per ES spec `new Date('2026-08-05')` is 2026-08-05T00:00:00Z, and date-fns `format` renders in local time, so in Eastern (UTC-4/-5) it renders '08/04'. This is not theoretical for this product \u2014 it is Eastern-anchored throughout (formatEastern/todayEastern imported at line 31, used for the report headers and filenames), so every deployment is in a negative offset. The file already imports the correct helper for this exact class of bug (`startOfLocalDay` from @/lib/dateLocal, line 35), and src/lib/dateLocal.js's header comment documents the UTC-midnight day-shift explicitly. Reachable via line 72 (chart preview) and line 879 (Trend Analysis CSV), and line 132 for the productivity chart. No test covers ReportsCenter. Impact is label-only \u2014 the underlying counts are bucketed correctly \u2014 but every row is attributed to the wrong day and the final bucket falls outside the stated Date Range header.",
   "corrected_severity": "medium",
   "corrected_fix": "Label from the local Date already in hand instead of re-parsing the string. Two surgical edits in /home/user/CareMetric-Pennsync/src/components/admin/ReportsCenter.jsx:\n\n1) generateTrendAnalysisData, lines 803-809 \u2014 change\n      const date = format(subDays(new Date(), i), 'yyyy-MM-dd');\n   to\n      const day = subDays(new Date(), i);\n      const date = format(day, 'yyyy-MM-dd');\n   and change line 809 `date: format(new Date(date), 'MM/dd'),` to `date: format(day, 'MM/dd'),`.\n\n2) generateProductivityReportData, lines 363-369 \u2014 identical change: hoist `const day = subDays(new Date(), i);`, keep `const date = format(day, 'yyyy-MM-dd');` for the string comparison on line 366, and change line 369 to `date: format(day, 'MM/dd'),`.\n\nThe bucket keys and all filtering stay byte-identical; only the display label changes, so no other behavior is affected."
  }
 ],
 "src/pages/UserManagement.jsx": [
  {
   "file": "src/pages/UserManagement.jsx",
   "symbol": "confirmToggleActive \u2014 offboarding audit write",
   "summary": "USER_DISABLED / USER_ENABLED UserActivity row is written before the offboardUser invoke, so a rejected or failed offboard leaves an audit entry asserting a disable that never happened.",
   "real": true,
   "reason": "Confirmed and reachable. UserManagement.jsx:271-277 calls logActivity(...USER_DISABLED, { offboarding: true }) and only then line 278 awaits base44.functions.invoke('offboardUser', args); the catch at :298-300 merely toasts and writes no compensating record. logActivity (src/components/utils/activityLogger.jsx:3-20) unconditionally creates a UserActivity row, so the record lands regardless of outcome. The 403 path is genuinely reachable: the client-side gate canOffboardUser (src/components/admin/userOffboarding.js:5-11) returns true for any role 'admin' / account_type 'agency_admin', while the server rejects with 403 'Only a super admin can offboard another administrator.' whenever the TARGET is privileged (offboardUser/entry.ts:79-84) \u2014 a check the client never replicates, so an agency_admin disabling another admin always produces exactly this contradiction. The same holds for a 400 'offboarding reason is required', 404 'User not found', any 500, or a dropped network connection. The server's own accurate audit uses a different action ('user_offboarded', entry.ts:232-246) and is written only after the work succeeds, so the pre-write client row is the only record that can lie, and it is the one keyed to the USER_DISABLED action an access-revocation review would filter on. The codebase's own convention is log-after-success \u2014 the sibling handleSaveUser logs USER_ROLE_CHANGED at :236 only after its invoke at :228 resolves. No test covers this ordering (runUserOffboard.test.js only exercises the pure payload builder).",
   "corrected_severity": "medium",
   "corrected_fix": "In src/pages/UserManagement.jsx, delete the logActivity block currently at lines 271-277 and re-insert it after the invoke and payload extraction \u2014 i.e. after `const payload = res?.data || res || {};` (line 280) and before the toast branch at line 281 \u2014 carrying the server outcome:\n\n      const res = await base44.functions.invoke('offboardUser', args);\n      queryClient.invalidateQueries({ queryKey: ['allUsersManagement'] });\n      const payload = res?.data || res || {};\n      logActivity(enabling ? ActivityActions.USER_ENABLED : ActivityActions.USER_DISABLED, {\n        user_email: selectedUser.email,\n        user_name: selectedUser.full_name,\n        entity_type: 'User',\n        entity_id: selectedUser.id,\n        offboarding: !enabling,\n        revocation_complete: payload.complete !== false,\n      });\n\nNothing else changes: logActivity stays fire-and-forget, the args/buildOffboardInvokeArgs call at 263-270 is untouched, and the toast branches at 281-295 keep their current order. Any throw from invoke now short-circuits before the audit write, so the log can no longer assert a disable that did not occur."
  }
 ],
 "base44/entities/ClinicalEvent.jsonc": [
  {
   "file": "base44/entities/ClinicalEvent.jsonc",
   "symbol": "ClinicalEvent (no rls block)",
   "real": true,
   "summary": "ClinicalEvent carries per-patient PHI but defines no rls block at all, and a client dashboard bulk-lists 200 rows of it unscoped (into an unused binding), so any authenticated user can read clinical events across every patient.",
   "reason": "CONFIRMED. Verified directly: base44/entities/ClinicalEvent.jsonc is 115 lines, ends at the `required` array, and `grep '\"rls\"'` returns nothing (exit 1). base44/config.jsonc contains no app-level default policy, so there is no global fallback. Every sibling patient-child entity in this repo does scope reads \u2014 Visit, PatientAlert and CarePlan all carry rls.read = {$or:[{created_by:{{user.email}}},{user_condition:{role:admin}}, ...]} \u2014 and base44/securityGuardrails.test.js:126-147 states the exact threat model ('without an rls.read policy any authenticated user could bulk-.list() every patient's rows'). Reachability is real and unguarded: src/components/alerts/PatientAlertsDashboard.jsx:108-112 runs `base44.entities.ClinicalEvent.list('-created_date', 200)` in user context on a dashboard non-admins open, and the result is bound to `_clinicalEvents` and never consumed anywhere in the file \u2014 pure cross-patient PHI egress (event_title / event_description / source_text free text, plus patient_id values usable for follow-on enumeration). src/components/patient/ClinicalEventsTimeline.jsx:77 `filter({ patient_id })` is likewise unscoped, so any authenticated user can pull any patient's event history by id. No test covers ClinicalEvent's RLS \u2014 it is one of 79 of 236 entity files with no rls block and was never added to PHI_READ_SCOPED_ENTITIES. I could not refute this.",
   "corrected_severity": "high",
   "corrected_fix": "Two surgical changes. (1) Delete the dead query in src/components/alerts/PatientAlertsDashboard.jsx lines 107-112 (the whole `const { data: _clinicalEvents = [] } = useQuery({ queryKey: ['clinicalEvents'], queryFn: () => base44.entities.ClinicalEvent.list('-created_date', 200), initialData: [] });` block, plus its 'Fetch clinical events for linking' comment). `_clinicalEvents` has no reader in the file, so this is zero-behavior-change and removes the 200-row cross-patient read outright. (2) Add an `rls` block to base44/entities/ClinicalEvent.jsonc as a sibling of `required`, mirroring PatientAlert.jsonc verbatim: \"rls\": { \"read\": { \"$or\": [ {\"created_by\": \"{{user.email}}\"}, {\"user_condition\": {\"role\": \"admin\"}} ] }, \"write\": { \"$or\": [ {\"created_by\": \"{{user.email}}\"}, {\"user_condition\": {\"role\": \"admin\"}} ] } }. IMPORTANT caveat on (2) that the original claim missed: every ClinicalEvent row is written through `base44.asServiceRole.entities.ClinicalEvent.create(...)` (base44/functions/extractClinicalEvents/entry.ts:177 and base44/functions/analyzeClinicalData/entry.ts:181), so confirm what `created_by` is populated with on a service-role create before shipping the read rule \u2014 if it is a system identity rather than the extracting clinician's email, a created_by-scoped read blanks ClinicalEventsTimeline on PatientDetails for every non-admin. If created_by is not the clinician, ship (1) now, make rls.read admin-only, and route ClinicalEventsTimeline's fetch through a server function that reuses the patient-access gate already written at base44/functions/extractClinicalEvents/entry.ts:32 (`user.role !== 'admin' && evPatient.created_by !== user.email && !evPatient.assigned_nurses?.includes(user.email)`). Do NOT use the claim's proposed OASISAssessment-shaped rule without that check. Optionally then add ClinicalEvent to PHI_READ_SCOPED_ENTITIES in base44/securityGuardrails.test.js to pin it."
  }
 ],
 "tools-live-readiness-report.mjs": [
  {
   "file": "tools-live-readiness-report.mjs",
   "symbol": "CLI direct-invocation guard",
   "summary": "Direct-invocation guard builds `file://${process.argv[1]}` by hand instead of using pathToFileURL, so on any checkout path needing percent-encoding the CLI body never runs and `pnpm run readiness:report` exits 0 having produced no report.",
   "real": true,
   "reason": "Verified line 44 verbatim: `if (import.meta.url === `file://${process.argv[1]}`) { process.exitCode = runLiveReadinessReportCli(); }`. Reproduced the failure directly: running a script under a directory named 'My Projects', `process.argv[1]` is the raw path while `import.meta.url` is percent-encoded (`My%20Projects`), so the naive comparison is false while `pathToFileURL(process.argv[1]).href` is true. Nothing refutes it: (a) no guard or fallback exists \u2014 the file has no other entrypoint, and package.json:33 wires `readiness:report` straight to `node tools-live-readiness-report.mjs`; (b) tools-live-readiness-report.test.mjs never exercises the guard \u2014 all five tests import `runLiveReadinessReportCli`/`buildLiveReadinessReportFromJson` and call them with an injected argv/readFile, so the passing suite says nothing about direct invocation; (c) the repo itself already treats this exact form as a known defect \u2014 tools-sync-shared-helpers.mjs:89-91 carries the comment 'a bare `file://${process.argv[1]}` can be a malformed URL and silently skip main()' and tools-sync-dedupe-engine.mjs:50 uses pathToFileURL. Two things narrow the real-world blast radius versus the claim: no GitHub workflow invokes readiness:report (grepped .github/ \u2014 no hits), so the 'deploy gate reads exit 0 as passed' scenario is hypothetical rather than wired up today; and the failure requires a checkout path containing a space/non-ASCII/reserved character, which the current environment does not have. The silent no-op is also somewhat self-announcing (zero stdout). Real but conditional and dev-tooling-scoped, hence severity lowered from medium to low.",
   "corrected_severity": "low",
   "corrected_fix": "In tools-live-readiness-report.mjs: add `import { pathToFileURL } from \"node:url\";` alongside the existing node:fs import at the top (line 2 area), then change line 44 from `if (import.meta.url === `file://${process.argv[1]}`) {` to `if (process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href) {`. Keep the `process.argv[1] &&` truthiness check because pathToFileURL(undefined) throws, which would break `import`-only consumers such as tools-live-readiness-report.test.mjs if argv[1] is ever absent. No other lines change; runLiveReadinessReportCli itself is untouched. Optionally apply the identical one-line change at tools-telnyx-live-smoke.mjs:142 (`const invokedDirectly = process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href;` plus the same node:url import), which already has the argv[1] guard and has the same defect."
  }
 ],
 "base44/functions/assignInService/entry.ts": [
  {
   "file": "base44/functions/assignInService/entry.ts",
   "symbol": "assignInService (dedupe against existing assignments)",
   "summary": "Dedupe keys only on (course_id, assigned_to_user_id) and ignores annual_cycle_year, so re-assigning the same annual course for a new cycle year creates zero assignments.",
   "real": true,
   "reason": "Code confirmed verbatim at entry.ts:49-61 and 73. No guard scopes the dedupe by cycle. The path is reachable: AnnualMandatoryEducationHub.jsx:32 sets `year = new Date().getFullYear()`, line 83 builds the selectable course list as `course.training_type === 'annual_mandatory' || course.annual_cycle_year === year` \u2014 so a prior-year annual_mandatory course still appears in the picker in the following year \u2014 and line 197 calls `assignInService({ courseId, dueDate, ..., annualCycleYear: year })`. `confirmModuleAssignment` (line 196-200) ignores the response entirely, so `assigned_count: 0 / skipped_existing: N` is never surfaced; the admin sees no error. The sibling function autoEnrollAnnualPlans/entry.ts:134 does scope its dedupe with `{ plan_id, annual_cycle_year: year }`, confirming year-scoping is the codebase's own intent and that assignInService is the outlier. No test covers assignInService dedupe (only base44/functions/activeUserGuardContract.test.js:50, which just asserts the auth guard). Mitigating: the canonical flow (seedYearlyRequiredInServices/entry.ts:1534) creates a NEW TrainingCourse per year keyed on (title, annual_cycle_year), so the built-in seeder path is unaffected \u2014 only the manual re-use-an-existing-course path breaks. That narrows real-world blast radius but does not refute the defect.",
   "corrected_severity": "medium",
   "corrected_fix": "In base44/functions/assignInService/entry.ts, hoist the target cycle before the dedupe block and post-filter on it. After line 49 add:\n  const targetCycleYear = annualCycleYear || course.annual_cycle_year || null;\nThen change line 57 from\n  assignedEmails = new Set(existingAssignments.map((assignment) => assignment.assigned_to_user_id));\nto\n  assignedEmails = new Set(existingAssignments.filter((a) => (a.annual_cycle_year ?? null) === targetCycleYear).map((a) => a.assigned_to_user_id));\nand change line 73 to `annual_cycle_year: targetCycleYear,`. Post-filter rather than adding annual_cycle_year to the `.filter({...})` call, so the null (non-annual) case still behaves exactly as today. Separately (optional, one line): have AnnualMandatoryEducationHub.jsx:197 surface the returned assigned_count/skipped_existing in a toast so a zero-assignment result can never be silent."
  }
 ],
 "src/components/training/LearningPathProgress.jsx": [
  {
   "file": "src/components/training/LearningPathProgress.jsx",
   "symbol": "LearningPathProgress \u2014 'plan-certificates' useQuery",
   "summary": "The certificates queryKey omits plan.year even though the queryFn filters on it, so concurrently-rendered plan cards for the same user share one cache entry.",
   "real": true,
   "reason": "Code confirmed at src/components/training/LearningPathProgress.jsx:38-46 (note: the claim's path `src/components/learning/LearningPathProgress.jsx` is wrong \u2014 the file lives under components/training/). The queryKey is genuinely missing an input its queryFn reads, which is an unambiguous react-query correctness defect, and every sibling query in the same file (lines 13, 22, 29) correctly includes its inputs. HOWEVER the claim's stated failure scenario is REFUTED: the only call site is MyAnnualEducationDashboard.jsx:216-222, and line 53 filters enrollments to `enrollment.plan_name?.includes(String(currentYear))`, so a learner's 2025 plan card never renders alongside the 2026 one. The residual reachable path is narrower: two plans can render together with different `plan.year` only when a plan's free-text name contains the current year while its year field differs \u2014 possible because LearningPlanForm.jsx:19-29 exposes `name` and `year` as fully independent user inputs (e.g. a '2026-2027 Annual Cycle' plan stored with year 2027 rendered next to '2026 Annual Nursing' with year 2026). In that case both cards show the same Certificates tile value. Impact is one secondary stat tile; the authoritative certificate list is the dashboard's Transcript tab (line 40-48), which is keyed correctly. Real but not high.",
   "corrected_severity": "low",
   "corrected_fix": "One line. src/components/training/LearningPathProgress.jsx:39: change `queryKey: ['plan-certificates', userId],` to `queryKey: ['plan-certificates', userId, plan?.year],`. No other change needed \u2014 `enabled` already gates on `!!plan?.year`."
  }
 ],
 "src/components/training/StaffEducationComplianceReport.jsx": [
  {
   "file": "src/components/training/StaffEducationComplianceReport.jsx",
   "symbol": "StaffEducationComplianceReport \u2014 table isAtRisk / generatePDF statusFor",
   "summary": "The on-screen table row and the PDF export drop the hasScores guard that the at-risk KPI and CSV export both apply, so staff with no scored work in the window are labeled At Risk with a phantom 0%.",
   "real": true,
   "reason": "Confirmed verbatim. Line 67: `avgScore = scored.reduce(...) / (scored.length || 1)` yields 0 when nothing is scored; line 79 sets `hasScores: scored.length > 0` with an explicit comment (lines 77-79) stating the guard exists precisely so 'a nurse with nothing scored isn't tagged At Risk with a phantom 0%'. Line 106 (`atRiskStaff`) and line 110-113 (`statusLabel`, used by the CSV at line 122) both honor `hasScores`. Line 308 (`const isAtRisk = staff.overdue > 0 || staff.avgScore < 70;`) and line 150-153 (`statusFor` inside generatePDF) both omit it. The trigger condition is trivially reachable \u2014 any active approved user (line 61) with no `score_percentage` on assignments inside the selected timeframe, i.e. every new hire and everyone whose only assignments are ungraded \u2014 and the timeframe defaults to 30 days. Result: the KPI card 'At-Risk Staff' and the table/PDF disagree within the same document, and the same staff member is 'On Track' in the CSV but 'At Risk' in the PDF. No test covers this component's badge rendering. Not refutable.",
   "corrected_severity": "medium",
   "corrected_fix": "Reuse the existing helper instead of re-deriving thresholds in two more places. (1) Delete lines 150-153 (the `statusFor` closure) and change line 176 from `statusFor(staff),` to `statusLabel(staff),`; change line 174 from `` `${staff.avgScore}%` `` to `` staff.hasScores ? `${staff.avgScore}%` : '-' ``. (2) Replace lines 308-309 with `const status = statusLabel(staff);` and update the JSX at lines 338-353 to branch on `status === 'Excellent'` / `status === 'At Risk'` / else. (3) Gate the score badge at lines 321-328 on `staff.hasScores`, rendering `<span className=\"text-slate-400\">-</span>` when false so an unscored nurse shows '-' instead of a destructive 0%. `statusLabel` is already defined at line 110 above both use sites, so no hoisting is required."
  }
 ],
 "src/components/hub-tabs/NurseTraining.jsx": [
  {
   "file": "src/components/hub-tabs/NurseTraining.jsx",
   "symbol": "NurseTraining \u2014 viewingModule branch / TrainingModuleViewer",
   "summary": "NurseTraining full-screen-replaces its UI with TrainingModuleViewer, which accepts only { module } and renders no Back or Complete control, trapping the user with no way out.",
   "real": true,
   "reason": "Confirmed. NurseTraining.jsx:86-97 early-returns ONLY the viewer, unmounting the tabs, stats and every other control. TrainingModuleViewer.jsx:175 is `export default function TrainingModuleViewer({ module })` \u2014 `nurseEmail`, `onComplete` and `onBack` are all dropped. A grep for `onBack|onComplete|Back|Button` across all 364 lines of TrainingModuleViewer.jsx returns zero hits, so there is genuinely no exit affordance and no Button component of any kind in the file. The trap is reachable from two call sites: OnboardingTracker.jsx:182 (`onStartModule(module)`) and PersonalizedLearningPath via NurseTraining.jsx:208-211, both wired to `handleStartModule` (line 76-79). The contrast is instructive: the other consumer, src/pages/TrainingCoursePlayer.jsx:565, renders `<TrainingModuleViewer module={...} />` inside its own chrome that supplies navigation \u2014 NurseTraining does not. One correction to the claim: `handleModuleComplete` (line 81-84) only calls setState; it writes no progress, so 'no progress is written' would remain true even if the callback fired. The defect is the inescapable screen, not lost progress.",
   "corrected_severity": "medium",
   "corrected_fix": "Minimal, contained to the call site so TrainingModuleViewer's signature and TrainingCoursePlayer are untouched. In src/components/hub-tabs/NurseTraining.jsx replace the block at lines 86-97 with a wrapper that renders the affordances itself:\n\n  if (viewingModule && selectedModule) {\n    return (\n      <div className=\"p-4 md:p-8 max-w-5xl mx-auto space-y-4\">\n        <Button variant=\"ghost\" onClick={() => { setViewingModule(false); setSelectedModule(null); }}>\n          Back to training\n        </Button>\n        <TrainingModuleViewer module={selectedModule} />\n        <div className=\"flex justify-end\">\n          <Button onClick={() => handleModuleComplete(null)}>Mark complete</Button>\n        </div>\n      </div>\n    );\n  }\n\nand add `import { Button } from \"@/components/ui/button\";` to the imports. Drop the `nurseEmail`/`onComplete`/`onBack` props from the viewer call since they are not part of its contract."
  }
 ],
 "src/components/training/LearnerMemoryBoosters.jsx": [
  {
   "file": "src/components/training/LearnerMemoryBoosters.jsx",
   "symbol": "LearnerMemoryBoosters.openBooster",
   "summary": "Memory-booster quiz reads TrainingQuestion directly from the browser, shipping correct_answer_json/rationale/rubric to any learner and defeating the answer-free getCoursePlayerQuestions payload.",
   "real": true,
   "reason": "Verified line 171 exactly as quoted: `base44.entities.TrainingQuestion.filter({ course_id: courseId, active: true }, \"order_index\", 50)` runs client-side under the learner's own token. Refutation attempts all failed: (a) base44/entities/TrainingQuestion.jsonc has NO `rls` block at all, so there is no read restriction and no field-level stripping \u2014 the full record, including `correct_answer_json`, `rationale`, `rubric`, `source_citations_json`, comes back over the wire; (b) TrainingCoursePlayer.jsx:130-152 shows the codebase's deliberate contract \u2014 it computes `canSeeAnswerKey = previewMode && getRoleView(currentUser) !== \"nurse\"` and explicitly comments that a learner must never get the raw fetch, routing everyone else through getCoursePlayerQuestions; the booster has no such gate; (c) getCoursePlayerQuestions/entry.ts:3-11 documents this exact leak as the reason the function exists; (d) the component is reachable \u2014 LearningCenter.jsx:1338 renders <LearnerMemoryBoosters /> for learners, and dueBoosters is populated from the learner's own passed assignments; (e) no spec file exists for this component (src/components/training has no LearnerMemoryBoosters spec), so no passing test covers the path. MicroQuiz.isCorrect (line 43) proves the answers really are held in client state. Same TrainingQuestion rows are reused for graded, certificate-issuing attempts, so the leak is not confined to the formative booster.",
   "corrected_severity": "medium",
   "corrected_fix": "Two-part, mirroring the existing player pattern: (1) In LearnerMemoryBoosters.jsx, import { getCoursePlayerQuestions } from \"@/functions/getCoursePlayerQuestions\" and replace line 171 with `const res = await getCoursePlayerQuestions({ course_id: courseId }); const qs = res?.data?.questions || [];` \u2014 the rest of openBooster (renderable filter on mcq/multi_select/true_false, sample(...,3)) is unchanged and still works, since that payload keeps id/type/prompt/options_json. (2) Because MicroQuiz then has no answer key, move booster grading server-side: add base44/functions/gradeMemoryBooster/entry.ts that auth-checks the caller, reads the questions with base44.asServiceRole, compares the submitted {questionId, answer} pairs, and returns `{ results: [{ question_id, correct, rationale }], score }`; change MicroQuiz.submit (lines 51-57) to await that call and set `graded` from the response, and render `q.rationale` (line 99) from the returned per-question rationale instead of the question object. Do not change the MicroLearningProgress write in completeBooster."
  }
 ],
 "src/components/learning/EmployeeTranscriptCenter.jsx": [
  {
   "file": "src/components/learning/EmployeeTranscriptCenter.jsx",
   "symbol": "EmployeeTranscriptCenter \u2014 certificates useQuery",
   "summary": "Employee/annual transcripts list and let the learner print or download REVOKED certificates; the filter omits `revoked: false` that every other read path applies.",
   "real": true,
   "reason": "Line 22 is exactly as quoted and has no `revoked` predicate; the only downstream filter (line 27-29) is a title search, and lines 113-114 wire Print/Download PDF to every row. AnnualTranscriptCenter.jsx:23 has the same omission (its only post-filter is `annual_cycle_year === currentYear`). Revocation is a first-class, supported state, not a hypothetical: base44/entities/TrainingCertificate.jsonc defines revoked/revoked_by/revoked_reason with `default: false`, issueCertificate/entry.ts:138 writes `revoked: false`, and every other consumer excludes revoked rows \u2014 LearningCenter.jsx:198-201 (`{ user_id, revoked: false }`), ceTranscript.js:91, generateLearningTranscriptPDF/entry.ts:31,96, generateCertificatePacketPDF:35, generateAndCacheCertificatePacket:89, exportLearningReportCSV:62,135, processTrainingRenewals:57, sendTrainingNotifications:105, CertificateExpirationReport.jsx:61. The TrainingCertificate RLS read rule (`user_id == user.email` OR admin) still returns the learner's own revoked rows, so nothing upstream filters them. Components are reachable (hub-tabs/EmployeeTranscript.jsx, LearningReports.jsx:104) and have no spec coverage.",
   "corrected_severity": "medium",
   "corrected_fix": "EmployeeTranscriptCenter.jsx line 22: `queryFn: () => base44.entities.TrainingCertificate.filter({ user_id: currentUser?.email, revoked: false }, '-issued_at', 300),` \u2014 matching LearningCenter.jsx:198-201. AnnualTranscriptCenter.jsx line 23: `const all = await base44.entities.TrainingCertificate.filter({ user_id: currentUser?.email, revoked: false }, '-issued_at', 500);` (leave the annual_cycle_year post-filter alone). No other behavior changes; both queries already key off currentUser?.email so no cache-key change is needed."
  }
 ],
 "src/components/training/MyAnnualEducationDashboard.jsx": [
  {
   "file": "src/components/training/MyAnnualEducationDashboard.jsx",
   "symbol": "MyAnnualEducationDashboard \u2014 'annual-courses' useQuery",
   "summary": "Two components register different query functions under the same [\"annual-courses\"] key \u2014 one pre-filtered to annual_mandatory, one unfiltered \u2014 so the Education Builder can render the filtered subset.",
   "real": true,
   "reason": "Both call sites are exactly as quoted (MyAnnualEducationDashboard.jsx:32-38 filtered; AnnualMandatoryEducationHub.jsx:78 unfiltered) and share the literal key. I tried to refute it on react-query semantics and could not: src/lib/query-client.js sets `staleTime: 60000` and `gcTime: 300000` globally with `refetchOnWindowFocus: false`, so when the admin switches from the \"My Education\" sub-tab to \"Education Builder\" (LearningCenter.jsx:1367-1379 and MyLearning.jsx:138-146 render them as sibling Radix TabsContent, which unmount when inactive) inside 60s, the hub mounts onto cached-but-fresh data and does NOT refetch \u2014 it renders the annual_mandatory-only subset. `initialData: []` cannot save it: initialData is only used when the cache is empty, and `initialDataUpdatedAt: 0` only affects the seeded-placeholder case. The concrete loss is real: the hub's memo (line 83) is `training_type === 'annual_mandatory' || annual_cycle_year === year`, so courses tagged for the cycle year with a different training_type disappear from the library, the module Select and the AnnualLearningPlanPanel (line 401). Scope correction to the claim: the two components are never mounted simultaneously (sibling tabs unmount), the reverse direction is harmless (the dashboard only uses `courses` for a courseMap lookup by assignment.course_id, line 59/154), and invalidateQueries refetches through whichever observer is currently mounted \u2014 so the wrong data self-corrects on the next hub invalidation or on a remount after 60s. That narrows impact but does not refute the collision. No test covers either component.",
   "corrected_severity": "low",
   "corrected_fix": "Change only the filtered variant's key so the two queries stop sharing a cache entry: MyAnnualEducationDashboard.jsx line 33 becomes `queryKey: [\"annual-courses\", \"mandatory-only\"],`. Leave AnnualMandatoryEducationHub.jsx:78 and all six of its `invalidateQueries({ queryKey: [\"annual-courses\"] })` calls untouched \u2014 react-query prefix-matches, so `[\"annual-courses\"]` invalidation still refreshes both entries, each through its own queryFn."
  }
 ],
 "src/components/learning/CertificateExpirationReport.jsx": [
  {
   "file": "src/components/learning/CertificateExpirationReport.jsx",
   "symbol": "CertificateExpirationReport \u2014 filtered useMemo",
   "summary": "The Business Line selector is rendered but never applied \u2014 `filtered`/`stats` use only filters.employee and windowDays, so Home Health \u2194 Hospice changes nothing on screen or in the CSV export.",
   "real": true,
   "reason": "Confirmed: `filters.businessLine` is initialized (line 38) and ReportFilters is rendered without `showBusinessLine={false}` (lines 131-141, and ReportFilters defaults `showBusinessLine = true`), yet the string `filters.businessLine` appears nowhere after line 38 \u2014 `filtered` (83-90) applies only the employee needle and the day window, `stats` (92-98) applies neither, and `handleExportCSV` (100-125) exports `filtered`. The contrast cases check out: OverdueRemindersReport.jsx:60 does apply `filters.businessLine` against `assigned_to_business_line`, and CourseRosterReport.jsx passes `showBusinessLine={false}` when it cannot. Nothing upstream constrains the data \u2014 both queries are unscoped `.list(..., 2000)`. One correction to the claim's fix: TrainingCertificate.jsonc does have a `business_line` field, but PersonnelCredential.jsonc has none, so half the rows are unfilterable and the suggested \"carry business_line onto items\" fix cannot be applied uniformly. No spec covers this component.",
   "corrected_severity": "low",
   "corrected_fix": "Minimal and honest: hide the control rather than fake it. In CertificateExpirationReport.jsx lines 131-141, drop the `businessLineOptions` prop and add `showBusinessLine={false}` (matching CourseRosterReport.jsx:140), and drop `businessLine: 'home_health'` from the useState default on line 38. If the business line must actually filter, do it for certificates only \u2014 add `business_line: c.business_line` to the certItems map (lines 62-69) and insert `.filter((i) => !filters.businessLine || filters.businessLine === 'all' || !i.business_line || i.business_line === filters.businessLine)` into `filtered`, add `filters.businessLine` to the dep array on line 90, and leave credentials unfiltered since PersonnelCredential has no business_line field. Handle PlanComplianceReport.jsx separately; it was not verified here."
  }
 ],
 "base44/functions/gradeTrainingAttempt/entry.ts": [
  {
   "file": "base44/functions/gradeTrainingAttempt/entry.ts",
   "symbol": "gradeTrainingAttempt \u2014 outer catch",
   "summary": "The two purpose-written \"your attempt was NOT recorded \u2014 please try again\" errors are swallowed by the outer catch and replaced with a generic 500 `Internal server error`.",
   "real": true,
   "reason": "Verified: the throw in gradeSubjectiveQuestions (line ~109) and the partial-AI-result throw (line ~229) both execute inside the `Deno.serve(async (req) => { try { ... } catch (error) { return Response.json({ error: 'Internal server error' }, { status: 500 }); } })` block that ends at line 417 \u2014 gradeSubjectiveQuestions is awaited at line 219 inside that try, so its throw propagates straight to the outer catch. The surrounding comments make the intent explicit (\"Surface the error so the attempt is NOT recorded and the learner can retry without consuming an attempt\"), and TrainingCoursePlayer.jsx:251-252 renders `err?.message || \"Failed to submit quiz. Please try again.\"`, so the learner gets a generic failure with no indication that the attempt was not consumed. There is no earlier catch, no error-shape branch, and no test for this function (base44/functions/gradeTrainingAttempt/ contains only entry.ts). The correctness behavior \u2014 attempt not recorded \u2014 is preserved, so this is a messaging defect only, which is why I lowered the severity.",
   "corrected_severity": "low",
   "corrected_fix": "Tag the two intentional failures and let the outer catch pass them through. At the throw site in gradeSubjectiveQuestions: `const e = new Error('AI grading is temporarily unavailable. Your attempt was not recorded \u2014 please try again.'); e.userFacing = true; throw e;` and the same two-line treatment for the 'AI grading did not return results for all questions...' throw. Then change the outer catch (lines 415-417) to: `console.error('gradeTrainingAttempt failed:', error); if (error?.userFacing) return Response.json({ error: error.message }, { status: 503 }); return Response.json({ error: 'Internal server error' }, { status: 500 });` \u2014 503 matches the existing precedent in appendPatientNoteHistory/entry.ts:163. No other code paths change; unexpected errors still return the generic 500."
  }
 ],
 "src/components/learning/EnrollmentSummaryDashboard.jsx": [
  {
   "file": "src/components/learning/EnrollmentSummaryDashboard.jsx",
   "symbol": "EnrollmentSummaryDashboard \u2014 'attempts-summary' useQuery and stats.avgScore",
   "summary": "The attempts query keys on the whole `filters` object while its query function ignores filters, and the resulting `avgScore` is computed but never rendered \u2014 every filter keystroke triggers an unused 1000-row refetch.",
   "real": true,
   "reason": "Verified line-by-line and could not refute. Line 40 is `queryKey: ['attempts-summary', filters]` while line 41 is `queryFn: () => base44.entities.TrainingAttempt.list('-submitted_at', 1000)` \u2014 the query function takes no filter arguments, so the key varies over data the fetch never uses. `ReportFilters` (src/components/learning/ReportFilters.jsx:32-36) has no debouncing: `handleFilterChange` builds a brand-new object and calls `onFilterChange(updated)` on every `onChange` of the Employee (line 94) and Course (line 106) text inputs, so each keystroke sets a new `filters` identity in the parent, producing a react-query cache key that has never been fetched; with the default staleTime of 0 and `initialData: []`, react-query issues a fresh network fetch for each new key. Typing a 10-character employee name therefore fires 10 identical 1000-row TrainingAttempt requests. The product of that work is genuinely dead: `grep avgScore` in the file returns only lines 48 and 57 (definition and object property) \u2014 the four rendered KPI cards (lines 113-159) show totalEnrolled, completed, completionRate and overdue only, and the `onExport` handler passed at line 106 is an empty stub, so nothing consumes `stats.avgScore`. No test covers this file (no test files reference EnrollmentSummaryDashboard). The secondary observation is also confirmed and is the more user-visible half: `showDateRange` defaults to true in ReportFilters (line 14) and is not overridden at the call site (lines 98-109), so From/To Date pickers are rendered, but the `assignments` memo at lines 29-37 filters only on status/employee/course/businessLine \u2014 an admin who sets a date range sees the KPIs and charts silently unchanged, i.e. a report that appears date-scoped but is not.",
   "corrected_severity": "low",
   "corrected_fix": "Two surgical edits in src/components/learning/EnrollmentSummaryDashboard.jsx. (1) Line 40: change `queryKey: ['attempts-summary', filters]` to `queryKey: ['attempts-summary']` \u2014 this alone stops the per-keystroke refetch storm and changes no rendered output, since the query function never used `filters`. (2) Remove the dead computation: delete lines 48-50 (`const avgScore = ...`) and the `avgScore` property on line 57, drop `attempts` from the dependency array on line 59, and delete the now-unused query at lines 39-43 \u2014 OR, if the KPI was intended, keep the query and render it by joining on assignment_id, e.g. `const ids = new Set(assignments.map(a => a.id)); const scoped = attempts.filter(t => ids.has(t.assignment_id));` and add a fifth KPI card reading `{stats.avgScore}%`. Separately (distinct one-line fix for the misleading date pickers): either pass `showDateRange={false}` in the `<ReportFilters ... />` props at lines 98-109, or add a date predicate inside the `assignments` memo, e.g. `if (filters.dateStart && a.created_date < filters.dateStart) return false; if (filters.dateEnd && a.created_date > filters.dateEnd) return false;`."
  }
 ],
 "src/components/notifications/NotificationCenter.jsx": [
  {
   "file": "src/components/notifications/NotificationCenter.jsx",
   "symbol": "NotificationCenter detail dialog \"Mark as read\"",
   "summary": "The detail dialog's \"Mark as read\" button omits the _synthetic guard applied everywhere else, so it calls Notification.update() with a PatientAlert id.",
   "real": true,
   "reason": "Confirmed at lines 319-330: the condition is `{!selectedNotification.is_read && (` with no _synthetic check, while the sibling Delete button at line 331 does check `!selectedNotification._synthetic`, markAllAsReadMutation filters on it (line 106), and handleNotificationClick guards on it (line 166). Synthetic items are reachable: lines 75-86 map critical/high active PatientAlerts into the list with `is_read: false` and `_synthetic: true`, and because handleNotificationClick refuses to mark them read, is_read is permanently false, so the button always renders when such an alert is opened. Clicking fires base44.entities.Notification.update(<PatientAlert id>, ...) at line 323. markAsReadMutation (lines 91-99) has no onError, so the failure is silent while the dialog closes at line 324. I discounted the claim's speculative \"writes to an unrelated Notification row\" escalation \u2014 there is no evidence the id spaces collide \u2014 so the concrete impact is a bogus cross-entity write plus a UI that falsely signals success.",
   "corrected_severity": "low",
   "corrected_fix": "Line 319: change `{!selectedNotification.is_read && (` to `{!selectedNotification.is_read && !selectedNotification._synthetic && (`, matching the guards at lines 106, 166 and 331. Optionally add `onError: () => toast.error('Could not mark this notification as read')` to markAsReadMutation (lines 91-99) so future failures are not silent; that requires importing toast from 'sonner', which this file does not currently do \u2014 skip it if you want the truly minimal change."
  }
 ],
 "src/components/scheduling/SmartRouteOptimizer.jsx": [
  {
   "file": "src/components/scheduling/SmartRouteOptimizer.jsx",
   "symbol": "SmartRouteOptimizer.optimizeRoute",
   "summary": "Every visit_id the LLM returns is written straight to Visit.update() without checking it belongs to the input visit set, and a single bad id rejects the whole Promise.all after some visits were already rewritten.",
   "real": true,
   "reason": "Confirmed at lines 140-144. The response_json_schema (lines 99-127) declares no `required` array, so an entry with a missing or hallucinated visit_id is schema-valid. useAICall (src/hooks/useAICall.js) is a pure timeout/retry + state wrapper \u2014 runWithRetry returns the raw InvokeLLM result with zero shape validation \u2014 so nothing between the model and the write checks membership. The only guard, at lines 132-135, checks solely that optimized_order is a non-empty array. Promise.all short-circuits on first rejection, so the catch at line 154 shows \"Couldn't optimize the route\" after an unknown subset of visit_time values has already been persisted, and neither optimizedRoute nor onOptimizedSchedule is set, leaving the nurse's day silently half-reordered with no record of which visits moved. A well-formed id outside the `visits` prop would be written unconditionally. Downgraded from the claim's framing only insofar as it requires model misbehavior; nothing in the code prevents it.",
   "corrected_severity": "medium",
   "corrected_fix": "Between the guard at line 135 and the write at line 140, insert: `const allowedIds = new Set((visits || []).map(v => v.id)); const writable = result.optimized_order.filter(v => allowedIds.has(v.visit_id) && /^\\d{2}:\\d{2}$/.test(v.suggested_time || ''));` and bail with the existing toast.error + return if `writable.length === 0`. Then replace lines 140-144 with `const outcomes = await Promise.allSettled(writable.map(v => base44.entities.Visit.update(v.visit_id, { visit_time: v.suggested_time })));` and after it `const failed = outcomes.filter(o => o.status === 'rejected').length + (result.optimized_order.length - writable.length); if (failed > 0) toast.warning(`${failed} visit(s) could not be rescheduled \u2014 check the schedule.`);`. Leave lines 146-152 (setOptimizedRoute / setSavings / onOptimizedSchedule) as-is so the UI still reflects what was persisted rather than claiming total failure after partial success."
  },
  {
   "file": "src/components/scheduling/SmartRouteOptimizer.jsx",
   "symbol": "optimizeRoute",
   "summary": "LLM-returned visit_id is written straight into Visit.update() with no membership check against the visits prop, and Promise.all leaves a half-applied schedule plus a stale UI when any update rejects.",
   "real": true,
   "reason": "Verified at src/components/scheduling/SmartRouteOptimizer.jsx:137-146. visitData is built from the `visits` prop (`visit_id: v.id`, line 47) but that id set is never reused. The only guard (lines 131-135) is an Array/length shape check on result.optimized_order \u2014 no identity check on visit_id and no check that suggested_time exists. The map body writes `base44.entities.Visit.update(v.visit_id, { visit_time: v.suggested_time })` for whatever the model returned. There is no caller-side validation: Dashboard.jsx:333 renders the component with raw visits, and the ai.run() response_json_schema (lines 99-128) types visit_id only as `{ type: \"string\" }` \u2014 no enum, no required \u2014 so any string passes. No test file exists for this component (only SmartRouteOptimizer.jsx in src/components/scheduling/). Concrete reachable failure: the model returns an id not in the current visit set (a stale/hallucinated id); Promise.all starts all N updates concurrently, the good ones commit, the bad one rejects, control jumps to the catch at line 152 which shows only \"Couldn't optimize the route. Please try again.\" \u2014 setOptimizedRoute/setSavings/onOptimizedSchedule never run, so the nurse sees the OLD ordering while some visit_times have already been overwritten in the DB. Pressing Optimize again re-runs the LLM against the already-mutated times and re-applies on top. The unvalidated-id write is the same defect from the other direction: if the echoed id happens to be a real visit outside the passed-in set, an unselected patient's visit_time is silently rewritten.",
   "corrected_severity": "medium",
   "corrected_fix": "In optimizeRoute, between the shape guard (line 135) and the write (line 139): add `const allowedIds = new Set((visits || []).map(v => v.id));` then `const updates = result.optimized_order.filter(v => allowedIds.has(v.visit_id) && v.suggested_time);` and `if (updates.length === 0) { toast.error('The optimizer returned an unusable result \u2014 please try again.'); return; }`. Replace the Promise.all block with `const outcomes = await Promise.allSettled(updates.map(v => base44.entities.Visit.update(v.visit_id, { visit_time: v.suggested_time })));` followed by `const failed = outcomes.filter(o => o.status === 'rejected').length; if (failed > 0) toast.warning(`${updates.length - failed} of ${updates.length} visits rescheduled \u2014 ${failed} could not be saved.`);`. Leave setOptimizedRoute/setSavings/onOptimizedSchedule where they are so the UI always reflects what was actually written. No other behavior changes."
  }
 ],
 "src/lib/offlineSync.js": [
  {
   "file": "src/lib/offlineSync.js",
   "symbol": "drainOnce (CREATE_VISIT branch)",
   "summary": "`NoteConversion` is permanently skipped when a CREATE_VISIT drain creates the Visit but throws before reaching the NoteConversion write, because the retry sees `wasExisting === true`.",
   "real": true,
   "reason": "Code confirmed verbatim at src/lib/offlineSync.js:120-153. In the CREATE_VISIT branch the order is: Visit.create (line 133) -> ComplianceAudit.filter/create (137-144) -> applyHistorySideEffect (147) -> NoteConversion.create (150-152) -> removeItem (153). Every step between the create and the NoteConversion write is a network call that can throw (ComplianceAudit.filter/create, and functions.invoke('appendPatientNoteHistory') inside applyHistorySideEffect at line 99). The catch at line 223 aborts the pass and leaves the item queued, so the next drain's Visit.filter({client_request_id}) at line 126 finds the just-created visit, sets wasExisting=true (line 130), and the `!wasExisting` guard at line 150 suppresses NoteConversion.create forever. The producer is real: src/components/smartNote/persistVisitNote.js:161 queues `__noteConversion: noteConversionFields` on the offline first-save path via upsertCreateVisitInSyncQueue, and NoteConversion is written nowhere else for that visit (the only other writer is the online path at persistVisitNote.js:259, which does not run for an offline save). No guard, default, or early return prevents this. The existing tests do not refute it: offlineSync.spec.js:66 covers the clean first-create and offlineSync.spec.js:98 covers the reuse path only in the 'prior drain fully succeeded' framing \u2014 neither exercises a drain that fails between Visit.create and NoteConversion.create. Impact is analytics-only (NoteConversion rows feed KPIDashboard / NoteConversionReport / statsCalculator), not clinical data, so severity stays low. NOTE: the claim's suggested fix is not viable as written \u2014 NoteConversion's schema (base44/entities/NoteConversion.jsonc) has no `visit_id` or `client_request_id` property, and base44/entityWriteDrift.test.js documents that Base44 silently drops unknown fields, so filtering/stamping on visit_id would write a field that never persists and could never be found on retry.",
   "corrected_severity": "low",
   "corrected_fix": "Reorder so the NoteConversion write happens immediately after the Visit is created, before any other network call can abort the pass. In src/lib/offlineSync.js, delete lines 148-152 (the `// NoteConversion only on first create...` comment plus the `if (!wasExisting && __noteConversion && entities.NoteConversion?.create) { await entities.NoteConversion.create(__noteConversion); }` block) and insert the same block directly after the `const visit = wasExisting ? existing[0] : await entities.Visit.create(visitPayload);` assignment (currently ending line 133) and before the `if (__audit) {` block at line 136, updating the comment to say the write is placed adjacent to the create so no intervening side effect can strand it. Keep the `!wasExisting` condition unchanged \u2014 this preserves both existing spec cases (offlineSync.spec.js:66 still sees the create, offlineSync.spec.js:98 still sees it skipped) and does not require a schema change. Do not add a visit_id/client_request_id-keyed filter unless NoteConversion.jsonc gains that property first, since Base44 drops unknown fields."
  }
 ],
 "src/components/documentation/RealTimeDocumentationReviewer.jsx": [
  {
   "file": "src/components/documentation/RealTimeDocumentationReviewer.jsx",
   "symbol": "RealTimeDocumentationReviewer props",
   "summary": "The apply-suggestion handler passed by the only caller is dead: the component destructures `_onApplySuggestion` (underscore-prefixed) and never invokes it anywhere.",
   "real": true,
   "reason": "Confirmed. src/components/documentation/RealTimeDocumentationReviewer.jsx:28 destructures `_onApplySuggestion`, and a full read of the file shows it is never referenced again \u2014 the only suggestion actions rendered are `copySuggestion` clipboard buttons (lines 442, 488, 531, 581), and a grep for 'Apply' in the file returns only line 28. The single caller, src/components/referral/AIAdmissionNoteGenerator.jsx:414-423, passes `onApplySuggestion={(suggestion) => { if (isEditing) setEditedNote(prev => prev + \"\\n\\n\" + suggestion); }}` \u2014 a different key, so it lands in nothing and never fires. Sibling components in the codebase (AutomaticDocumentReviewer.jsx:504-551, AISmartOASISAssistant.jsx:468, ProactiveDocumentationAssistant.jsx:187) all take the non-underscored `onApplySuggestion` and actually call it, confirming the intended contract and that this one is the outlier. No test covers it. This is a dead-feature/lint-artifact issue, not a crash or data-integrity problem, and the clinician retains the clipboard path, so impact is cosmetic-to-minor.",
   "corrected_severity": "low",
   "corrected_fix": "Prefer the delete-the-dead-prop direction, which is the truly minimal change: remove line 28 (`_onApplySuggestion,`) from the destructure in RealTimeDocumentationReviewer.jsx and remove the `onApplySuggestion={(suggestion) => {...}}` prop from the caller at src/components/referral/AIAdmissionNoteGenerator.jsx:419-423, so no one mistakes the handler for working behavior. If the feature is actually wanted instead, rename line 28 to `onApplySuggestion,` and add, next to each existing copy Button (the one at lines 439-446 for `issue.suggestion` is the primary one), a guarded sibling: `{onApplySuggestion && (<Button size=\"sm\" variant=\"ghost\" onClick={() => onApplySuggestion(issue.suggestion)} className=\"h-6 px-2\">Apply</Button>)}` \u2014 matching the guarded pattern already used at src/components/review/AutomaticDocumentReviewer.jsx:504-508. Do not do both; pick one."
  }
 ],
 "src/components/review/AutomaticDocumentReviewer.jsx": [
  {
   "file": "src/components/review/AutomaticDocumentReviewer.jsx",
   "symbol": "AutomaticDocumentReviewer (autoReview effect)",
   "summary": "Sole call site passes `documentContent` while the component destructures `noteContent`, so the auto-review effect never fires and the component renders null \u2014 the whole compliance-review feature is dead.",
   "real": true,
   "reason": "Confirmed verbatim. AutomaticDocumentReviewer.jsx:32-43 destructures only `noteContent, visitType, diagnosis, patientData, vitalSigns, visitId, nurseEmail, autoReview, onReviewComplete, onApplySuggestion` \u2014 no `documentContent`/`documentType`/`compact`. Line 296-300 is `if (autoReview && noteContent && noteContent.length > 100 && !reviewResults) performReview();`. Line 330 `if (!reviewResults && !autoReview)` renders the manual card, and line 352 is `if (!reviewResults) return null;`. `grep -rn AutomaticDocumentReviewer src/` returns exactly one usage: OASISAnalyzer.jsx:2015-2017 with `documentType=\"oasis_assessment\"` and `documentContent={JSON.stringify(pdgmData\u2026)}`, plus `autoReview={true}`. So `noteContent` is `undefined` \u2192 effect guard false \u2192 `autoReview` true skips the manual branch \u2192 `return null`. No prop-forwarding wrapper, no defaultProps, no test covers this component (no test file references it). Nothing refutes it. (Cited call-site line numbers are 2015-2017, not 2004-2006; code text matches.)",
   "corrected_severity": "medium",
   "corrected_fix": "Rename the prop at the single call site: in src/components/hub-tabs/OASISAnalyzer.jsx line 2017 change `documentContent={JSON.stringify(pdgmData, null, 2) + '\\n\\n' + JSON.stringify(analysisResults, null, 2)}` to `noteContent={\u2026same expression\u2026}`. Leave the rest of the JSX alone (`documentType`/`compact` are ignored extra props and harmless; delete them only if desired). Optionally harden the component by changing line 330 to `if (!reviewResults && (!autoReview || !noteContent || noteContent.length <= 100))` so a future wiring regression shows the manual 'Start Comprehensive Review' card instead of rendering nothing."
  }
 ],
 "src/components/reports/KPIDashboard.jsx": [
  {
   "file": "src/components/reports/KPIDashboard.jsx",
   "symbol": "KPIDashboard.inPreviousPeriod",
   "summary": "`inPreviousPeriod` parses date-only fields as UTC midnight while the window bounds are local, so boundary-day records are counted in both the current and previous period and the previous period's own first day is dropped.",
   "real": true,
   "reason": "Confirmed. Lines 148-151 are exactly `const date = new Date(item[dateField]);` compared against `previousStart`/`rangeStart`, which are built at lines 84-85 as `new Date(dateRange.start + 'T00:00:00')` (local). The current-period path at lines 90-95 deliberately normalizes date-only strings to local midnight with an explicit comment about this exact hazard, so the two paths are inconsistent by construction. The data really is date-only: `referral_date` in base44/entities/Referral.jsonc is an unconstrained string and DocumentToTriageMapper.jsx:166 writes `todayEastern()` which is `formatInTimeZone(new Date(), EASTERN_TIMEZONE, 'yyyy-MM-dd')` (timezone.js:68-70); `assessment_date` is declared `format: \"date\"`. In UTC-4/-5, a record dated the range start lands at 20:00 the prior day, satisfying `>= previousStart && < rangeStart`, so it is counted in both `filteredReferrals` and `prevReferrals` \u2014 and records dated `previousStart`'s day fall below `previousStart` and are dropped. No test covers KPIDashboard. Impact is limited to the trend badges (`referralTrend`, `oasisTrend`), not the headline counts, which is why I lowered severity.",
   "corrected_severity": "low",
   "corrected_fix": "Hoist the existing normalization out of `filterByDate` and reuse it. Above line 86 add `const parseItemDate = (raw) => { const s = String(raw || ''); return new Date(/^\\d{4}-\\d{2}-\\d{2}$/.test(s) ? s + 'T00:00:00' : s); };` then replace line 94's inline expression with `const itemDate = parseItemDate(item[dateField]);` and line 149's `const date = new Date(item[dateField]);` with `const date = parseItemDate(item[dateField]);`. No other behavior changes."
  }
 ],
 "src/components/ui/SearchablePatientSelect.jsx": [
  {
   "file": "src/components/ui/SearchablePatientSelect.jsx",
   "symbol": "SearchablePatientSelect (CommandEmpty branches)",
   "summary": "Both `<CommandEmpty>` blocks are unreachable because `shouldFilter={false}` makes cmdk set `filtered.count` to the total registered item count, and the 'Add new patient' item is always registered.",
   "real": true,
   "reason": "Confirmed against the installed library. `<Command shouldFilter={false}>` at line 240; the two `<CommandEmpty>` branches at lines 248-268; an unconditional `<CommandGroup className=\"border-t\"><CommandItem value=\"__add_new_patient__\" \u2026>` at lines 399-407. In node_modules/cmdk@1.1.1 dist/index.mjs the empty component is `\u2026filtered.count===0)?t.createElement(D.div,{\u2026,\"cmdk-empty\":\"\"}):null`, and the filter routine begins `function J(){\u2026if(!n.current.search||p.current.shouldFilter===!1){n.current.filtered.count=u.current.size;return}` \u2014 with filtering disabled, count equals the number of registered items, which is >= 1 here always. So neither empty state can ever render. No test exercises this component. Impact is a missing explanatory message, not data loss, hence low severity.",
   "corrected_severity": "low",
   "corrected_fix": "Inside `<CommandList>` (lines 248-268) swap the two `<CommandEmpty>` wrappers for plain `<div>`s, keeping the identical guard conditions: `{localPatients.length === 0 ? (<div className=\"py-6 text-center\"> \u2026 </div>) : favoritesList.length === 0 && recentList.length === 0 && allPatientsList.length === 0 ? (<div className=\"py-6 text-center\"><p className=\"text-sm text-muted-foreground mb-3\">No patients match your search.</p></div>) : null}` and drop the now-unused `CommandEmpty` import. Nothing else changes."
  }
 ],
 "src/components/clinical/VitalsChart.jsx": [
  {
   "file": "src/components/clinical/VitalsChart.jsx",
   "symbol": "VitalsChart (isLoading branch)",
   "summary": "`initialData: []` makes the query report success immediately, so `isLoading` is permanently false: the skeleton is dead code and the card asserts 'No completed visits with vital signs recorded yet' while the fetch is still in flight.",
   "real": true,
   "reason": "Confirmed. Lines 22-27 include `initialData: []`; line 44 `if (isLoading)`; lines 48-56 render the definitive empty-state text. React Query v5 computes `isLoading = isPending && isFetching`, and seeded `initialData` sets `status: 'success'` so `isPending` is false. I checked the app's client config (src/lib/query-client.js): `staleTime: 60000` with `initialDataUpdatedAt: 0`, which deliberately forces the fetch to run on mount \u2014 so the query does fetch, but during that window the component shows the false empty state rather than the skeleton. PhysicianDirectory.jsx:28-32 has the same `initialData: []` with an `isLoading` gate at line 87. No test covers either component. Downgraded to low because the wrong state is transient and self-corrects when data lands.",
   "corrected_fix": "In src/components/clinical/VitalsChart.jsx delete line 26 (`initialData: [],`) \u2014 the `data: visits = []` destructure at line 22 already supplies the empty default, and the global `initialDataUpdatedAt: 0` in src/lib/query-client.js exists only to undo this pattern's fetch suppression, so removing it changes nothing else. Apply the identical one-line deletion at src/components/physician/PhysicianDirectory.jsx line 32.",
   "corrected_severity": "low"
  }
 ],
 "src/components/dashboard/UpcomingTelehealthWidget.jsx": [
  {
   "file": "src/components/dashboard/UpcomingTelehealthWidget.jsx",
   "symbol": "UpcomingTelehealthWidget.upcoming",
   "summary": "The 'upcoming' list filters only on status, never on time, so past never-ended sessions sort to the top (ascending) and permanently occupy the four visible slots and the header count.",
   "real": true,
   "reason": "Code confirmed verbatim, though at lines 21-25 and 48, not the claimed 191/218 (the file is 78 lines). `upcoming` filters `status === 'scheduled' || 'active'`, sorts ascending on `scheduled_at || 0`, and line 48 renders `upcoming.slice(0, 4)`; line 37 prints `{upcoming.length} upcoming visit{\u2026}`. I checked every writer of the status field: `completed` is only set by the explicit end-session action (src/pages/Telehealth.jsx:138) and `cancelled` only by the manual cancel handler (Telehealth.jsx:160); there is no scheduled job or backend transition, and the entity default is `scheduled` (base44/entities/TelehealthSession.jsonc). So a no-show row stays `scheduled` indefinitely and, being the oldest, sorts first. Not refuted. Severity lowered to low: the consequence is a misleading dashboard tile, and the linked room is still reachable from the Telehealth page.",
   "corrected_severity": "low",
   "corrected_fix": "Add a time bound to the existing filter at lines 21-23 while keeping live calls visible: `const cutoff = Date.now() - 60 * 60 * 1000;` then `const upcoming = sessions.filter((s) => s.status === \"active\" || (s.status === \"scheduled\" && s.scheduled_at && Date.parse(s.scheduled_at) >= cutoff)).sort((a, b) => new Date(a.scheduled_at || 0) - new Date(b.scheduled_at || 0));`. The existing `if (upcoming.length === 0) return null;` then correctly hides the widget when only stale rows remain; no other component is affected (the widget is used once, at src/pages/Dashboard.jsx:271)."
  }
 ],
 "src/components/oasis/AIGeneratedOASISAssessment.jsx": [
  {
   "file": "src/components/oasis/AIGeneratedOASISAssessment.jsx",
   "symbol": "oasis_items render filters (All/Functional/Clinical/Cognitive tabs)",
   "summary": "M1021/M1023 are deliberately retained at generation time but unconditionally dropped by all four tab filters, so the AI primary diagnosis is saved but can never be reviewed or edited.",
   "real": true,
   "reason": "All quoted code confirmed verbatim: the generation filter keeps 1021/1023 (line 84, with an explicit comment saying they are kept 'so the primary diagnosis is saved and the referral-divergence safety check can run'), while the four render filters at lines 328, 452, 468 and 484 return false for the whole 1000-1060 range with no exception. Consequences are reachable with no guard anywhere: (a) the m1021Divergence banner (lines 47-56, rendered at 233-241) explicitly instructs 'Verify the primary diagnosis against the referral before finalizing', but the M1021 row is absent from every tab, so there is no expandable row and no Textarea (lines 385-401) to correct it - editingItems can never receive a key for M1021; (b) saveAssessment (lines 105-129) maps over assessment.oasis_items, not the filtered list, so the unreviewed AI M1021 response is persisted to OASISAssessment.oasis_items and counts toward completion_percentage/status. No test covers this component. This is a genuine mismatch between two filters in the same file.",
   "corrected_severity": "medium",
   "corrected_fix": "In AIGeneratedOASISAssessment.jsx, change the four render predicates to match line 84: line 328 `if (num >= 1000 && num <= 1060) return false;` -> `if (num >= 1000 && num <= 1060 && num !== 1021 && num !== 1023) return false;`, and identically at lines 452, 468 and 484. Cleanest minimal form: extract the existing predicate as a module-level helper, e.g. `const isAdministrativeItem = (itemNumber) => { const m = String(itemNumber || '').toUpperCase().match(/M(\\d+)/); if (!m) return false; const num = parseInt(m[1]); return num >= 1000 && num <= 1060 && num !== 1021 && num !== 1023; };` and use `!isAdministrativeItem(item.item_number)` in the generate filter (line 84) and all four tabs. No other behavior changes."
  }
 ],
 "src/components/oasis/SmartNoteDataImport.jsx": [
  {
   "file": "src/components/oasis/SmartNoteDataImport.jsx",
   "symbol": "extractFunctionalData - ambulation branch order",
   "summary": "The ambulation keyword chain tests the generic 'requires assistance' before 'bedbound'/'non-ambulatory', so the most impaired patients get the lower M1860 score, which is then written over the OASIS-extracted value.",
   "real": true,
   "reason": "Code confirmed verbatim at lines 72-80: independent -> device (1) -> requires assistance (3) -> bedbound/non-ambulatory (6). Since if/else-if short-circuits on first match, a note containing both 'bedbound' and the generic phrase 'requires assistance' (extremely common phrasing: 'bedbound, requires assistance with all ADLs') yields score 3, and 'uses walker ... now largely non-ambulatory' yields 1. Nothing guards this: the same function is used for both the preview (line 197) and the imported payload (line 59), and the consumer OASISAnalyzer.jsx:2292-2305 writes obs.ambulation.score straight into updatedScores.m1860_ambulation and setPdgmData, overwriting the score extracted from the OASIS PDF. No test covers SmartNoteDataImport. Mitigation that caps severity: the import is user-initiated and the preview at lines 200-204 shows the description ('Requires assistance') before the clinician clicks import, so the wrong value is visible, though the numeric score and the overwrite of the PDF-extracted M1860 are not.",
   "corrected_severity": "low",
   "corrected_fix": "SmartNoteDataImport.jsx lines 72-80: reorder the ambulation chain most-severe-first so a more specific match wins: keep `if (lowerNarrative.includes('bedbound') || lowerNarrative.includes('non-ambulatory')) { observations.ambulation = { score: 6, description: 'Bedbound' }; }` as the FIRST branch, then `else if ('requires assistance' || 'assist with ambulation')` -> score 3, then `else if ('uses walker' || 'with device')` -> score 1, then `else if ('ambulates independently' || 'walks independently')` -> score 0. Only the order of the four existing branches changes; scores, descriptions and keywords stay as-is."
  }
 ],
 "src/components/oasis/ProactiveRescoringEngine.jsx": [
  {
   "file": "src/components/oasis/ProactiveRescoringEngine.jsx",
   "symbol": "Re-analyze Opportunities button",
   "summary": "The Re-analyze button has no disabled prop, so it can be clicked repeatedly while a request is in flight, firing duplicate paid Opus calls.",
   "real": true,
   "reason": "Partly confirmed, but the claimed data-integrity failure is refuted. The button at 369-375 genuinely has no `disabled` prop, and it stays mounted during a re-analysis because `{ai.loading && <spinner>}` (line 187) and `{opportunities && (...)}` (line 194) are independent blocks, not exclusive branches \u2014 so a second click is reachable. useAICall's reqIdRef only gates the hook's own data/loading/error state; `run` resolves for every caller, and setOpportunities at 126 is unguarded. However the stale-overwrite harm is not reachable in practice: the single call site (OASISAnalyzer.jsx:1580-1588) passes oasisData=pdgmData, clinicalContext=analysisResults?.summary and a no-op onOpportunitiesFound, and those props cannot change while the user sits on the results panel, so both in-flight calls analyze identical input and either result is equally current \u2014 no wrong revenue numbers, no stale parent state. What is real is the duplicated expensive claude_opus_4_8 call (the same cost the file's other comments at 33-34 and 137-139 go out of their way to prevent) and a possible result flicker. Low severity, and the reqId ref the claim asks for is unnecessary.",
   "corrected_severity": "low",
   "corrected_fix": "One-line change: add `disabled={ai.loading}` to the Re-analyze Button at ProactiveRescoringEngine.jsx:369-374 (matching the `!opportunities && !ai.loading` guard already used on the initial Analyze button at line 175). Do not add a request-id ref in the component \u2014 useAICall already ignores stale hook state, and with the button disabled no overlapping call can be started."
  }
 ],
 "src/components/hub-tabs/ComplianceMonitoringDashboard.jsx": [
  {
   "file": "src/components/hub-tabs/ComplianceMonitoringDashboard.jsx",
   "symbol": "handleNotifySelected",
   "summary": "handleNotifySelected dereferences groupedByUser[email] for selections that filters have since removed, throwing a TypeError and killing the notify action.",
   "real": true,
   "reason": "CONFIRMED. Line 238 is verbatim `const userData = groupedByUser[email];` followed by `userData.issues.map(...)` at 239 with no guard. `groupedByUser` (line 208-209) comes from deriveComplianceIssueStats built over the FILTERED issue list (src/components/compliance/complianceIssueStats.js:39). Grep for `setSelectedUsers` in this file shows it is only set by handleToggleUser (213), handleSelectAll (223-227) and the mutation's onSuccess \u2014 there is no pruning effect. The Notify button (line 383) is enabled on `selectedUsers.size > 0` alone. So: Select All with severityFilter='all', then switch severityFilter to 'high' (or categoryFilter to 'overdue_training') drops credential-only/medium-only users out of groupedByUser while their emails stay selected, and clicking Notify throws `Cannot read properties of undefined (reading 'issues')`. React does not route event-handler throws to error boundaries, so the admin sees a dead button. Decisive evidence: the sibling copy of this exact screen, src/pages/ComplianceCenter.jsx:291-298, already carries the pruning fix with the comment 'Prune selections that fall out of view when filters change, so a stale selected email can't reach handleNotifySelected with no matching issue data' \u2014 the hub-tab copy was never updated. No test covers this file.",
   "corrected_severity": "medium",
   "corrected_fix": "Port the proven fix from src/pages/ComplianceCenter.jsx:291-298. (1) Change line 1 import to `import React, { useState, useEffect } from \"react\";`. (2) Immediately after the deriveComplianceIssueStats destructure (after line 209) insert:\n\n  // Prune selections that fall out of view when filters change, so a stale\n  // selected email can't reach handleNotifySelected with no matching issue data.\n  useEffect(() => {\n    setSelectedUsers(prev => {\n      const next = new Set(Array.from(prev).filter(email => groupedByUser[email]));\n      return next.size === prev.size ? prev : next;\n    });\n  }, [groupedByUser]);\n\nThis keeps the Notify count honest as well as preventing the throw. Optionally also harden line 238 with `if (!userData) return null;` and a `.filter(Boolean)` before `.join('\\n\\n')`, but the prune alone closes the reachable path."
  },
  {
   "file": "src/components/hub-tabs/ComplianceMonitoringDashboard.jsx",
   "symbol": "complianceIssues \u2192 recentVisits",
   "summary": "The recent-visits window has no lower bound, so future-dated scheduled visits are counted as recent and flagged as incomplete documentation.",
   "real": true,
   "reason": "CONFIRMED. Lines 157-161 are verbatim `return differenceInDays(today, visitDate) <= 7;` with no `>= 0` bound and no status filter. For a visit_date in the future, differenceInDays(today, future) is negative and passes. The visits query (lines 67-72) is `Visit.filter({}, '-visit_date', 500)` \u2014 unfiltered, so future rows are present. Future scheduled visits are the ordinary workflow: src/pages/PatientDetails.jsx:76-81 initializes newVisit with `status: 'scheduled'`, line 765-769 is a free `<Input type=\"date\">` the user edits, and line 131 creates it as-is; the reset at 136-141 keeps `status: 'scheduled'`. Such rows have no nurse_notes, so line 176-178 increments userIncompleteVisits for every one, producing an `incomplete_documentation` issue at 100% / severity 'high' (line 190) against the scheduling nurse, inflating the Incomplete Docs stat and, if the admin clicks Notify, emailing that nurse about visits that have not happened. Nothing else in the file or in ComplianceCenter.jsx bounds the window (ComplianceCenter builds no recentVisits at all, so there is no corrected sibling). No test covers this path.",
   "corrected_severity": "medium",
   "corrected_fix": "Add the lower bound only \u2014 the minimal change that fixes future-dating without altering how past undocumented visits are treated. Replace line 160 with:\n\n      const daysAgo = differenceInDays(today, visitDate);\n      return daysAgo >= 0 && daysAgo <= 7;\n\nDo NOT also drop `status === 'scheduled'` wholesale: a past-dated visit still sitting in 'scheduled' with no notes is genuinely missing documentation and should keep being flagged."
  }
 ],
 "src/components/hub-tabs/FaxContacts.jsx": [
  {
   "file": "src/components/hub-tabs/FaxContacts.jsx",
   "symbol": "handleCSVUpload / normalizeFax",
   "summary": "CSV export adds a formula-injection guard apostrophe to every field, but import strips it only from the fax column, so name/organization/notes accumulate a leading apostrophe on each export\u2192import cycle.",
   "real": true,
   "reason": "Verified all three code sites. escapeCsvField (src/components/admin/csvExport.js:21) prefixes ' to ANY field matching /^[=+\\-@\\t\\r]/, and it is applied to every cell via toCsvRows, which handleExportCSV (FaxContacts.jsx:220-224) uses for Name, Fax Number, Organization and Notes. On import, parseCsv (FaxContacts.jsx:42-83) does no unescaping \u2014 it treats ' as an ordinary character \u2014 and normalizeFax (line 189) is applied only to values[faxIndex] (line 194); name/organization/notes are stored verbatim at lines 199-202. A notes value like '- Prefers fax before 3pm' contains no comma/quote so it is not even RFC-quoted, meaning the raw cell literally begins with an apostrophe and re-imports that way. No guard, no caller-side sanitization (bulkCreate stores as given), and no test exercises FaxContacts CSV import \u2014 no *.spec/test file references FaxContacts at all. Corruption compounds on each round trip since the stored ''- ... is re-escaped on the next export. Not a lint-enforced rule.",
   "corrected_severity": "low",
   "corrected_fix": "In src/components/hub-tabs/FaxContacts.jsx, replace line 189 with a guarded generic unescaper and apply it to every imported cell:\n\n  const unescapeFormulaGuard = (raw) => (raw || \"\").replace(/^'(?=[=+\\-@\\t\\r])/, \"\").trim();\n\nThe lookahead is important: it only removes the apostrophe our exporter could have added, so an organization legitimately named \"'Round the Clock Care\" is untouched. Then in the loop (lines 194-203):\n  const name = unescapeFormulaGuard(values[nameIndex]);\n  const faxNumber = unescapeFormulaGuard(values[faxIndex]);\n  if (name && /\\d/.test(faxNumber)) {\n    contactsToAdd.push({\n      name,\n      fax_number: faxNumber,\n      organization: orgIndex !== -1 ? unescapeFormulaGuard(values[orgIndex]) : \"\",\n      notes: notesIndex !== -1 ? unescapeFormulaGuard(values[notesIndex]) : \"\"\n    });\n  }\nNo other behavior changes (the digit requirement and name requirement are preserved)."
  }
 ],
 "src/components/hub-tabs/RealTimeComplianceDashboard.jsx": [
  {
   "file": "src/components/hub-tabs/RealTimeComplianceDashboard.jsx",
   "symbol": "useQuery({ queryKey: ['oasisUploads'] })",
   "summary": "Four components share React Query key `['oasisUploads']` with four different queryFns/limits (50 via a field-stripping backend function, 200, 500, 500 via raw entity list), so whichever mounts first within the 60s staleTime supplies the cached data for the others.",
   "real": true,
   "reason": "All four call sites confirmed exactly as quoted: RealTimeComplianceDashboard.jsx:58-61 (entity list, 500), OASISAnalyzer.jsx:200-205 (functions.invoke('listOASISUploads'), limit 50), OASISAnalyticsDashboard.jsx:55-57 (entity list, 200), PDGMTrendDashboard.jsx:64-66 (entity list, 500). `staleTime: 60000` is confirmed at src/lib/query-client.js:48 and `refetchOnWindowFocus: false` at line 40, so a second observer mounting on the same key inside 60s renders the cached payload and issues no fetch \u2014 its own queryFn never runs. Nothing keys off the limit, and no `select`/normalization reconciles the two shapes. Note PDGMAnalyticsDashboard.jsx:47 already uses a disambiguated key `['oasisUploads','analytics']`, showing the codebase knows the pattern \u2014 these four are the outliers. Reachability is not exotic: OASISAnalyzer and OASISAnalyticsDashboard are sibling tabs of the same OASISCenter page, so a plain tab switch computes analytics aggregates from the Analyzer's 50-row payload. There is an extra hazard the claim understates: the Analyzer's queryFn exists specifically to strip financial fields server-side for non-financial users (comment at lines 202-203); when a raw `OASISUpload.list` observer populates the key first, the Analyzer's saved list renders unstripped rows. `invalidateQueries({queryKey:['oasisUploads']})` at line 211 prefix-matches, so the suggested key change does not break the save path.",
   "corrected_severity": "medium",
   "corrected_fix": "Give each fetch a key that encodes source and limit, keeping `'oasisUploads'` as the shared prefix so existing invalidation still matches: OASISAnalyzer.jsx:201 \u2192 `queryKey: ['oasisUploads', 'fn', 50]`; OASISAnalyticsDashboard.jsx:56 \u2192 `['oasisUploads', 'list', 200]`; RealTimeComplianceDashboard.jsx:59 \u2192 `['oasisUploads', 'list', 500]`; PDGMTrendDashboard.jsx:65 \u2192 `['oasisUploads', 'list', 500]` (these last two may intentionally share, as their queryFns are byte-identical). Leave OASISAnalyzer.jsx:211's `invalidateQueries({ queryKey: ['oasisUploads'] })` unchanged \u2014 prefix matching still invalidates all four."
  }
 ],
 "src/components/hub-tabs/DocumentSignatures.jsx": [
  {
   "file": "src/components/hub-tabs/DocumentSignatures.jsx",
   "symbol": "View signed PDF button",
   "summary": "Signed-PDF links open via bare `window.open(url, '_blank')`, bypassing the project's `openExternalUrl` helper and thus both scheme validation and `noopener,noreferrer`.",
   "real": true,
   "reason": "Line 273 confirmed verbatim; `openExternalUrl` (security.jsx:97-104) and `isSafeExternalUrl` (77-88) exist as quoted and are the established pattern at 10+ sites (FaxLogsDashboard.jsx:438, DocumentList.jsx:114, TemplateLibrary.jsx:241/315, etc.). I tried to refute on provenance and could not: `signed_pdf_url` is normally written server-side from a platform upload (embedAnnotationsToPDF/entry.ts:176, submitSignerSignature/entry.ts:162), but base44/entities/DocumentSignature.jsonc:293-313 grants entity `write` to `created_by == {{user.email}}`, so any authenticated staff account can create a DocumentSignature row and set `signed_pdf_url` to an arbitrary string via the client SDK \u2014 while `read` grants admins visibility of all rows. That is a reachable insider path from a low-privilege user to an admin clicking View in a PHI app. Severity is held down, not up, by two things: the attack requires a crafted SDK/API write (no UI field sets `signed_pdf_url` \u2014 SignatureRequestCreator.jsx:175 does not), and `javascript:` handling in `window.open` varies by browser. The `window.opener` gap is unconditional though: unlike anchor `target=\"_blank\"`, `window.open` does not get implicit noopener, so any opened page keeps a live handle to the PHI-bearing SPA. Note this is one of a dozen unguarded `window.open` sites (CertificateDownloadButton.jsx:16/28, SignerDocumentSigner.jsx:131, PDFTemplateManager.jsx:373, TemplateVersionHistory.jsx:58, IncidentReportingModule.jsx:740), so fixing only this line leaves the class open.",
   "corrected_severity": "low",
   "corrected_fix": "Add `import { openExternalUrl } from \"@/components/utils/security\";` to DocumentSignatures.jsx and change line 273 to `onClick={() => openExternalUrl(sig.signed_pdf_url)}`. Behavior is unchanged for the normal platform-issued https URL. Apply the same one-line substitution to the sibling entity-URL sites for consistency: SignerDocumentSigner.jsx:131, PDFTemplateManager.jsx:373, TemplateVersionHistory.jsx:58, CertificateDownloadButton.jsx:16 and :28. Leave the `window.open('', '_blank')` print-window calls alone \u2014 they open a blank document, not a URL."
  }
 ],
 "src/pages/LearningCenter.jsx": [
  {
   "file": "src/pages/LearningCenter.jsx",
   "symbol": "downloadTranscript",
   "summary": "A failed transcript-PDF request is swallowed into console.error with no user-facing message, so the button silently does nothing after showing \"Preparing...\".",
   "real": true,
   "reason": "Confirmed at src/pages/LearningCenter.jsx:359-377; code matches the quote exactly. The catch block only calls console.error and the finally clears the spinner, so a non-2xx response (5xx, auth expiry, backend error) or a throw from response.arrayBuffer()/downloadBlob leaves zero user-facing signal. The only early return is `if (!user?.email) return;` at line 360, which does not guard the fetch outcome. The button JSX at lines 513-519 renders only a spinner state and an idle state -- there is no error branch and no state variable capturing failure. No test file for LearningCenter exists and nothing in src/ references downloadTranscript or 'Transcript PDF', so no passing test covers this path. Notably this path is the outlier on its own page: every other error path here does surface feedback (enrollFeedback[course.id] === 'error' at line 1129, enrollError at line 1163, ratingFeedback at line 1438). One detail in the claim's evidence is inaccurate and worth flagging: it asserts 'Every other failure path on this page uses toast.error', but LearningCenter.jsx imports no toast at all -- its other paths use inline state feedback. The toast comparison is nonetheless apt against the sibling page the code comment at lines 363-364 explicitly names as the pattern source: src/pages/UserGuides.jsx:43-45 has the identical `if (!response.ok) throw new Error(...)` plus console.error AND `toast.error('Failed to generate guide. Please try again.')`. The fetch/arrayBuffer mechanics were copied but the user-facing error half was dropped. This inaccuracy does not refute the underlying defect. toast is viable: SonnerToaster is mounted globally at src/App.jsx:270.",
   "corrected_severity": "low",
   "corrected_fix": "Two minimal edits to src/pages/LearningCenter.jsx, matching the established UserGuides.jsx pattern.\n\n1. Add the import alongside the existing imports at the top of the file (after line 9, `import { Tabs, ... } from '@/components/ui/tabs';`):\n\n    import { toast } from 'sonner';\n\n2. Add the user-facing signal in the existing catch block. Change lines 372-374 from:\n\n    } catch (error) {\n      console.error('Failed to download transcript:', error);\n    } finally {\n\nto:\n\n    } catch (error) {\n      console.error('Failed to download transcript:', error);\n      toast.error('Failed to generate your transcript PDF. Please try again.');\n    } finally {\n\nDo not touch the `finally { setDownloadingTranscript(false); }` block -- it already correctly clears the spinner on both success and failure. Do not alter the `if (!user?.email) return;` guard or the button's disabled condition. Scope is confined to the catch block plus one import; no other behavior changes."
  }
 ],
 "src/pages/IncidentReportingModule.jsx": [
  {
   "file": "src/pages/IncidentReportingModule.jsx",
   "symbol": "createIncidentMutation.mutationFn",
   "summary": "The \"Office Notified\" checkbox the reporter fills in is never sent to the backend; the server derives office_notified from immediate_alert (severity === 'high'), so the stored compliance field contradicts what the nurse entered.",
   "real": true,
   "reason": "Confirmed as quoted and unrefuted. formData carries office_notified (init line 89, reset line 247, checkbox lines 546-553), handleSubmit spreads ...formData into the mutation (line 226), but mutationFn enumerates fields explicitly (lines 131-144) and omits office_notified. Server base44/functions/submitIncidentReport/entry.ts:63 writes `office_notified: !!payload.immediate_alert` and line 143 sets immediate_alert = (severity === 'high'), so the field is fully derived from severity: every high-severity incident is stored office_notified=true even when the box is unchecked, and every medium/low incident is stored false even when checked. office_notified is a real, defaulted boolean in base44/entities/Incident.jsonc:69-73. No guard, no caller-side normalization, no test covers it (grep for office_notified across src/base44/e2e returns only the sites cited plus updateIncident/entry.ts:222, which sets it true on review transitions \u2014 a separate path that does not repair the submission value).",
   "corrected_severity": "high",
   "corrected_fix": "Two-line change. (1) src/pages/IncidentReportingModule.jsx line 142: after `physician_notified: incidentData.physician_notified,` add `office_notified: incidentData.office_notified,`. (2) base44/functions/submitIncidentReport/entry.ts line 63: change `office_notified: !!payload.immediate_alert,` to `office_notified: !!payload.office_notified,`. Leave line 64 (`alert_triggered: !!payload.immediate_alert`) and the admin-notification block unchanged so severity-driven alerting is unaffected. office_notified already exists in the Incident schema, so the entityWriteDrift/schemaContract guardrails stay green."
  }
 ],
 "src/pages/Messages.jsx": [
  {
   "file": "src/pages/Messages.jsx",
   "symbol": "threads / filteredThreads",
   "summary": "Thread inbox membership is computed only from the latest message, so a participant addressed on an earlier message in the same thread loses the whole conversation, including its unread messages and its contribution to the unread badge.",
   "real": true,
   "reason": "Code confirmed at lines 176-177 and 183-184; the same latest-message-only predicate also drives the header badge (lines 279-281). The failure needs a thread whose messages address different people over time, and the app produces exactly that: src/pages/ReferralIntake.jsx:419 pins `thread_id: `referral-${referralId}`` with `recipients: [nurseEmail]`, so reassigning a referral to a second nurse appends a message to the same thread addressed only to the new nurse \u2014 the first nurse's assignment notice (possibly unread) vanishes from her inbox. src/components/documents/ReferralDocumentViewer.jsx:60 does the same with `referral-doc-${id}` and a chosen single recipient, so sending the same referral document to a second person hides it from the first. The New Message dialog is single-recipient (line 537) and handleReply targets only the latest sender/recipients (lines 251-255), so nothing repairs membership. Message.list('-created_date', 200) at line 70 is unfiltered on the client side, so the data is present and only this predicate hides it. No test exercises this (no test file references isRecipient/filteredThreads).",
   "corrected_severity": "medium",
   "corrected_fix": "src/pages/Messages.jsx lines 176-177, inside the threads map (sortedMessages is already in scope): change to `isMyMessage: sortedMessages.some(m => m.sender_email === currentUser?.email),` and `isRecipient: sortedMessages.some(m => m.recipients?.includes(currentUser?.email)),`. Nothing else needs to change \u2014 handleReply and the header/preview still read thread.latestMessage, so reply addressing and sort order are untouched."
  }
 ],
 "src/pages/ReferralFollowUp.jsx": [
  {
   "file": "src/pages/ReferralFollowUp.jsx",
   "symbol": "providerFax prefill useEffect",
   "summary": "The provider-fax prefill effect keys on the `selected` object identity, so any ['referrals'] refetch re-runs it and overwrites the fax number the user just typed (clearing it, or silently reverting it to the stale directory value).",
   "real": true,
   "reason": "Confirmed at lines 171-182 with deps [selected, physicians]; `selected` is a plain `reviewable.find(...)` (line 138) off a useMemo on [referrals] (lines 115-124). React Query structural sharing does not save this: persistRequest performs `Referral.update(selected.id, { follow_up_requests: persisted })` then `invalidateQueries({ queryKey: [\"referrals\"] })` (lines 329-330), so the refetched row genuinely differs, the array and the selected object are new references, and the effect re-runs. generateAndPersistPortalLink (lines 338-349) hits this while the user is on the same screen with the fax input mounted, so a fax typed into `fu-provider-fax` (line 809) is replaced by `match?.fax_number || \"\"`. Two reachable outcomes: the field is cleared and the Fax button is disabled (faxDisabled uses !providerFax.trim(), line 844) / faxToProvider errors \"Enter the provider's fax number first.\" (line 371); or, worse and silently, a user-corrected number reverts to the stale directory number and the PDF is faxed there. The reset effect at line 149 already uses the stable selectedId, showing the intended pattern.",
   "corrected_severity": "medium",
   "corrected_fix": "Hoist the derived name out so the dependency is a stable string. Above the effect add: `const referringPhysicianName = selected ? normName(selected.extracted_data?.demographics?.referring_physician) : \"\";` Then in the effect body replace `const refName = selected ? normName(...) : \"\";` with `const refName = referringPhysicianName;` (or use the hoisted value directly) and change line 182 to `}, [selectedId, referringPhysicianName, physicians]);`. This keeps the mandatory reset-on-selection-change behavior (selectedId and the name both change when the referral changes, and refName becoming \"\" still clears the field) while making a follow_up_requests refetch a no-op. Do not simply drop to [selectedId] alone \u2014 the effect must still re-run when the physicians directory finishes loading."
  }
 ],
 "src/pages/ReferralIntake.jsx": [
  {
   "file": "src/pages/ReferralIntake.jsx",
   "symbol": "handleProcessingComplete / automationSummary",
   "summary": "The \"New patient created\" summary branch is unreachable, so auto-creating a brand-new chart from a referral is reported to intake staff as \"Patient matched\", sometimes with a confidence percentage.",
   "real": true,
   "reason": "Confirmed dead. The auto-create block (guarded by `!existingPatient && !updates.requires_manual_review`, line 733) sets both `updates.patient_id = newPatient.id` and `existingPatient = newPatient` (lines 792-793); the only other assignment of updates.patient_id (line 823) is inside `else if (existingPatient)`. So `!existingPatient && updates.patient_id` at line 976 can never hold. Since the create branch also requires requires_manual_review to be false, the first branch (line 974) always fires for it and prints `\u2713 Patient matched: First Last` plus `(N% confidence)` when updates.match_confidence was set by the low-confidence AI analysis (lines 693-696) \u2014 i.e. the most misleading wording lands exactly on the create path. Nothing else in the flow re-labels it; the only cue is a different toast string, so intake staff are told a chart was matched when one was created.",
   "corrected_severity": "low",
   "corrected_fix": "Track creation explicitly. Next to the existing `let existingPatient` declaration add `let createdNewPatient = false;`, and inside the auto-create branch after line 793 (`existingPatient = newPatient;`) add `createdNewPatient = true;`. Then reorder lines 974-978 to test creation first: `if (createdNewPatient) { automationSummary.push('\u2713 New patient chart created'); } else if (existingPatient && !updates.requires_manual_review) { automationSummary.push(`\u2713 Patient matched: ${existingPatient.first_name} ${existingPatient.last_name}${updates.match_confidence ? ` (${Math.round(updates.match_confidence)}% confidence)` : ''}`); }`. No data writes change."
  },
  {
   "file": "src/pages/ReferralIntake.jsx",
   "symbol": "handleProcessingComplete (analysisTasksToCreate)",
   "summary": "A next step whose priority is 'high' passes the filter but the ternary maps it to Task priority 'medium', silently downgrading it.",
   "real": true,
   "reason": "Verified at src/pages/ReferralIntake.jsx:871-878. The filter admits `immediate | urgent | high`; the ternary at line 878 tests only `immediate | urgent`, so a `high` step is written with `priority: 'medium'`. The value is producible: base44/functions/analyzeReferralIntake/entry.ts:75 instructs the model to emit `\"priority\": \"immediate|urgent|high|medium|low\"`, and the response is not enum-constrained, so `high` is the expected middle value \u2014 the filter author clearly intended to keep it. The target enum supports it: base44/entities/Task.jsonc:36-42 defines priority as `critical | high | medium | low`, so `'high'` was available and no schema constraint forces the downgrade. No caller normalizes priority afterward. Concrete failure: a 'high' clinical follow-up (e.g. obtain wound-care orders before the first visit) is created as a medium task, sorting below genuinely medium work and invisible to any view filtering `priority === 'high'`. Real, but the task is still created and still visible, so impact is prioritization only.",
   "corrected_severity": "low",
   "corrected_fix": "Minimal, behavior-preserving for the existing immediate/urgent path: at src/pages/ReferralIntake.jsx:878 change `priority: step.priority === 'immediate' || step.priority === 'urgent' ? 'high' : 'medium',` to `priority: 'high',` \u2014 the filter on line 872 already admits only immediate/urgent/high, so every surviving step is a high-priority action and no other value can reach this line. (If the three-way distinction is wanted, use `? 'critical' : 'high'` instead, but note that escalates today's immediate/urgent tasks from high to critical, which is a behavior change for anything keyed on 'critical'.)"
  }
 ],
 "src/pages/CarePlanManagement.jsx": [
  {
   "file": "src/pages/CarePlanManagement.jsx",
   "symbol": "handleSaveBuilder",
   "summary": "The Care Plan Builder's plan-name Input is live and editable but its value is never written anywhere, so a clinician-typed plan name is silently discarded on save.",
   "real": true,
   "reason": "Confirmed: `planName` has exactly two references in the file \u2014 useState at line 284 and `value={planName}` on the editable Input at line 400 (with an onChange handler). handleSaveBuilder's payload (lines 353-360) contains only patient_id, problem, goal, interventions, frequency, status, and both the create and update paths use that same object. base44/entities/CarePlan.jsonc has no plan-name property, and per base44/entityWriteDrift.test.js the platform silently drops unknown keys, so there is no hidden write path either. The success toast (line 370) reports interventions only, so there is no signal the name was dropped. No test references planName/plan_name. Clinical content is still saved, so this is a lost label rather than lost documentation.",
   "corrected_severity": "low",
   "corrected_fix": "Pick one of two surgical options. (A) Persist it: add a `\"plan_name\": { \"type\": \"string\", \"description\": \"Clinician-entered care plan name\" }` property to base44/entities/CarePlan.jsonc (required \u2014 the write-drift guardrail and Base44 both reject unknown keys), then add `plan_name: planName,` to the `data` object in handleSaveBuilder at line ~355, and seed `setPlanName` from an existing plan when the builder loads one so reopening does not show \"New Care Plan\". (B) If the name is not meant to be stored, make the UI honest: replace the `<Input value={planName} onChange=...>` at lines 398-403 with a static heading (e.g. `<h2 className=\"h-8 text-sm font-semibold\">{builderPatient ? `${builderPatient.first_name} ${builderPatient.last_name} \u2014 Care Plan` : 'Care Plan'}</h2>`) and drop the unused planName/setPlanName state at line 284."
  }
 ],
 "base44/functions/sendDocumentReminderEmails/entry.ts": [
  {
   "file": "base44/functions/sendDocumentReminderEmails/entry.ts",
   "symbol": "Deno.serve reminder loop",
   "summary": "The 'already reminded today' guard differences a midnight-normalized `today` against the raw `last_reminder_sent_at`, so any reminder sent yesterday suppresses today's \u2014 cadence halves and the due_today reminder is skipped.",
   "real": true,
   "reason": "Verified at base44/functions/sendDocumentReminderEmails/entry.ts:170-207. `today` is normalized (`today.setHours(0,0,0,0)`, line 171) but `lastSent` (line 202) is not, and the difference is floored (lines 203-205) then compared `< 1` (line 206). Arithmetic check: reminder stamped Tue 06:05Z (line 282 writes `new Date().toISOString()`), next run computes Wed 00:00 \u2212 Tue 06:05 = 17.9h \u2192 0.746 days \u2192 Math.floor = 0 \u2192 `continue`. Any reminder sent at any hour of the previous calendar day suppresses the current day; the guard only passes on the second day after. Reachable on the normal path: with the default `reminder_days_before` of 3 (line 210), pre_due fires at D-3, is blocked at D-2, fires at D-1, and is then blocked on the due date itself \u2014 the `due_today` branch (lines 213-214) never sends. Overdue reminders then alternate. No guard prevents this: the function is a real cron endpoint (schedulerAuth, lines 3-38, comment at 166) and the only tests referencing this function (base44/securityGuardrails.test.js, base44/functions/activeUserGuardContract.test.js) assert auth/guard contracts, not reminder-window arithmetic. The sibling base44/functions/sendAutomatedSignatureReminders/entry.ts uses an elapsed-time window instead, confirming the intended semantics.",
   "corrected_severity": "medium",
   "corrected_fix": "One-line fix at base44/functions/sendDocumentReminderEmails/entry.ts:202 \u2014 after `const lastSent = new Date(pkg.last_reminder_sent_at);` add `lastSent.setHours(0, 0, 0, 0);` so both sides of the subtraction are calendar midnights and `daysSinceReminder < 1` means literally 'already sent today'. (Equivalent alternative matching the sibling function: replace lines 202-207 with `const lastSentMs = new Date(pkg.last_reminder_sent_at).getTime(); if (lastSentMs && (Date.now() - lastSentMs) < 20 * 60 * 60 * 1000) continue;`.) Nothing else in the loop changes."
  }
 ],
 "base44/functions/autoRetryFailedFaxes/entry.ts": [
  {
   "file": "base44/functions/autoRetryFailedFaxes/entry.ts",
   "symbol": "notifyFaxExhausted (Notification.create)",
   "summary": "The 'all fax retries exhausted' notification deep-links to `/send-fax`, which is neither a route nor a redirect, so the action button lands on PageNotFound.",
   "real": true,
   "reason": "Verified at base44/functions/autoRetryFailedFaxes/entry.ts:523: `action_url: `/send-fax?fax_id=${fax.id}``. Routes are derived from NAV_MANIFEST page names (src/routes.jsx:46-65), and the fax page is registered as `SendFax` (src/lib/nav.manifest.js:328, src/pages/SendFax.jsx). Rendered paths are `/${name}` (src/App.jsx:137). React Router matches case-insensitively but not punctuation-insensitively, so the hyphen makes `/send-fax` unmatched, and it is not in REDIRECTS (src/routes.jsx:70-200 \u2014 the fax entries are /FaxDashboard, /FaxLogsDashboard, /FaxContacts, /FaxAddressBook, /FaxAnalytics; none is /send-fax). It therefore falls through to `<Route path=\"*\" element={<PageNotFound />} />` (src/App.jsx:180/245). The URL is consumed as-is by NotificationCenter.jsx:313 (`<Link to={selectedNotification.action_url}>`) with no normalization. The sibling notifications in base44/functions/handleTelnyxStatusWebhook/entry.ts:783 and :795 correctly use `/SendFax?...`, confirming the intended value. Reachable whenever a fax exhausts its retries and `fax.sent_by` is set.",
   "corrected_severity": "low",
   "corrected_fix": "One-line change at base44/functions/autoRetryFailedFaxes/entry.ts:523 \u2014 `action_url: `/send-fax?fax_id=${fax.id}`` becomes `action_url: `/SendFax?fax_id=${fax.id}``, matching the routed page name and the handleTelnyxStatusWebhook sibling. No other edits."
  }
 ],
 "base44/functions/sendCredentialRenewalReminders/entry.ts": [
  {
   "file": "base44/functions/sendCredentialRenewalReminders/entry.ts",
   "symbol": "Deno.serve credential loop",
   "summary": "`daysUntilExpiry` parses the date-only `expiration_date` as UTC midnight but subtracts the current wall-clock `today`, then floors \u2014 so the value is consistently one day short (a credential expiring today reports -1) and every reminder tier fires a day early.",
   "real": true,
   "reason": "CONFIRMED as quoted. entry.ts:174 `const today = new Date();` (carries time-of-day), entry.ts:199-200 `const expirationDate = new Date(cred.expiration_date); const daysUntilExpiry = Math.floor((expirationDate - today) / (1000*60*60*24));`. `new Date('2026-08-05')` is spec-defined as UTC midnight, so for any cron run after 00:00Z the difference is negative-fractional and `Math.floor` rounds away from zero: a credential expiring today yields -1, one expiring in 8 days yields 7. Nothing upstream normalizes this \u2014 the only guards in the loop are `if (!cred.expiration_date || cred.status === 'expired') continue` (line 197) and the `$gte/$lte` window filter (lines 181-189), neither of which corrects the arithmetic. The value is rendered verbatim to staff at line 253 `['Days remaining', String(daysUntilExpiry)]` and drives the tier filter at line 225 `daysUntilExpiry <= offset`. I confirmed the sibling job sendPersonnelExpirationNotifications/entry.ts:198-199 computes the same quantity correctly (`new Date(`${item.expiration_date}T00:00:00Z`)` + `Math.ceil`), so the two crons genuinely disagree by one day on the same credential. No test file exists for sendCredentialRenewalReminders (searched *.test.* under base44/functions \u2014 only deduplicatePatients has tests), so nothing pins the current behavior. Could not refute. Impact is a wrong number in a compliance email plus tiers firing a day early; no data loss and no missed notification, so real but low severity.",
   "corrected_severity": "low",
   "corrected_fix": "In base44/functions/sendCredentialRenewalReminders/entry.ts, replace lines 199-200 with:\n      const expirationDate = new Date(`${cred.expiration_date}T00:00:00Z`);\n      const daysUntilExpiry = Math.ceil((expirationDate.getTime() - today.getTime()) / (1000 * 60 * 60 * 24));\nThis matches sendPersonnelExpirationNotifications/entry.ts:198-199 exactly. Leave line 252's separate `new Date(cred.expiration_date).toLocaleDateString()` and the tier/digest thresholds untouched \u2014 no other behavior changes."
  }
 ],
 "src/components/oasis/OASISExportManager.jsx": [
  {
   "file": "src/components/oasis/OASISExportManager.jsx",
   "symbol": "exportToPDF / exportToCSV",
   "summary": "Both export handlers swallow every failure with only `console.error`, so a failed export is visually indistinguishable from one the user never triggered \u2014 no toast, no error state, no banner.",
   "real": true,
   "reason": "CONFIRMED exactly as quoted: OASISExportManager.jsx:179-181 (`catch (error) { console.error('CSV export error:', error); }`) and :216-218 (`catch (error) { console.error('PDF export error:', error); }`), each followed by `setIsExporting(false); setExportType(null);`. I read the full component: the only imports are React/lucide/base44/toCsvRows/toLocalISODate \u2014 `toast` is not imported at all \u2014 and the render block (lines 239-283) has exactly one status signal, `exportSuccess`, which is only ever set on the success path (lines 177, 214). There is no error state variable and no error UI anywhere in the component, so a 500 from generateOASISReportPDF (or the explicit `throw new Error(`PDF generation failed (${response.status})`)` at line 201) leaves the button silently reverting to its idle label. That is the deliberate-throw path, so it is plainly reachable. The codebase pattern is the opposite everywhere else \u2014 src/pages/Help.jsx:52-54 wraps the identical `functions.fetch` PDF-download flow with `toast.error('Failed to download manual: ' + message)`, and ~10 sibling components in src/components/oasis/ call toast.error in their catches. Not linter-enforced, no test covers it. Real, but a missing-feedback UX defect with no data-integrity consequence.",
   "corrected_severity": "low",
   "corrected_fix": "In src/components/oasis/OASISExportManager.jsx: add `import { toast } from \"sonner\";` after line 9. Then change line 180 to:\n      console.error('CSV export error:', error);\n      toast.error('Failed to export the CSV. Please try again.');\nand line 217 to:\n      console.error('PDF export error:', error);\n      toast.error('Failed to generate the PDF report. Please try again.');\nNo other change \u2014 the `setIsExporting(false); setExportType(null);` resets and the success-banner logic stay as-is."
  }
 ]
}