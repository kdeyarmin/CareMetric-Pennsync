{
  "name": "Incident",
  "type": "object",
  "properties": {
    "patient_id": {
      "type": "string",
      "description": "Reference to Patient entity"
    },
    "visit_id": {
      "type": "string",
      "description": "Reference to Visit entity"
    },
    "incident_type": {
      "type": "string",
      "enum": [
        "fall",
        "hospitalized",
        "medication_error",
        "behavioral_change",
        "infection_suspected",
        "refusal_of_care",
        "pressure_injury",
        "emergency_visit"
      ],
      "description": "Type of incident"
    },
    "incident_name": {
      "type": "string",
      "description": "Display name of incident"
    },
    "incident_date": {
      "type": "string",
      "format": "date",
      "description": "Date incident occurred"
    },
    "incident_time": {
      "type": "string",
      "description": "Time incident occurred"
    },
    "severity": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high"
      ],
      "description": "Severity level"
    },
    "details": {
      "type": "object",
      "description": "Incident-specific details"
    },
    "report": {
      "type": "string",
      "description": "Full incident report text"
    },
    "physician_notified": {
      "type": "boolean",
      "default": false,
      "description": "Whether physician was notified"
    },
    "office_notified": {
      "type": "boolean",
      "default": false,
      "description": "Whether office was notified"
    },
    "status": {
      "type": "string",
      "enum": [
        "reported",
        "under_review",
        "resolved"
      ],
      "default": "reported",
      "description": "Incident status"
    },
    "resolution_notes": {
      "type": "string",
      "description": "Notes on how incident was resolved"
    },
    "ai_tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "AI-generated tags for categorization and trend analysis"
    },
    "is_sample": {
      "type": "boolean",
      "default": false,
      "description": "Whether this is sample data visible to all users"
    }
  },
  "required": [
    "patient_id",
    "incident_type",
    "incident_date"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "is_sample": true
        }
      ]
    },
    "write": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}