[build]
builder = "DOCKERFILE"
dockerfilePath = "services/integration-runtime/Dockerfile"
watchPatterns = ["services/integration-runtime/**", "railway.toml"]

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
